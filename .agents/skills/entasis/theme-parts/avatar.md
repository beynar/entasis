# avatar theme parts

Generated by `npm run generate:component-contract` from `Avatar`'s theme file. Do not edit.

- Registry key: `avatar`, the key under `<Theme components>`
- Setter: `setAvatarTheme` from `entasis/avatar`
- Parts: 5. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `relative items-center border border-neutral-muted text-neutral aspect-ratio-1 rounded-full`
- `size`: normal → `size-8` · small → `size-6` · large → `size-10`

## `avatarImage`

- Element: `<img>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `rounded-full absolute top-0 left-0 z-0 object-cover w-full max-w-full h-full max-h-full`
- `size`: normal → _none_ · small → _none_ · large → _none_

## `avatarPrefix`

- Element: `<Slot>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `absolute bottom-[-0.25rem] rounded-full border border-neutral-muted p-[0.25rem] bg-surface size-4 left-[-0.25rem] aspect-square`
- `size`: normal → `size-4 right-[-0.3rem] bottom-[-0.2rem]` · small → `size-5 right-[-0.25rem] bottom-[-0.25rem]` · large → `size-5 left-[-0.4rem] bottom-[-0.4rem]`

## `avatarSuffix`

- Element: `<Slot>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `absolute  rounded-full border border-neutral-muted p-[0.25rem] bg-surface aspect-square`
- `size`: normal → `size-4 right-[-0.3rem] bottom-[-0.3rem]` · small → `size-3.5 left-[-0.25rem] bottom-[-0.25rem]` · large → `size-3.5 right-[-0.4rem] bottom-[-0.4rem]`

## `avatarInitials`

- Element: `<div>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `absolute bg-surface-floating bottom-0 w-full h-full text-center left-0 rounded-full flex items-center justify-center uppercase font-bold text-sm`
- `size`: normal → `text-sm` · small → `text-xs` · large → `text-base`
