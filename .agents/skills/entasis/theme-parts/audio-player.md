# audio-player theme parts

Generated by `npm run generate:component-contract` from `AudioPlayer`'s theme file. Do not edit.

- Registry key: `audio-player`, the key under `<Theme components>`
- Setter: `setAudioPlayerTheme` from `entasis/audio-player`
- Parts: 30. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="audio-player">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `group/audio-player @container relative grid w-full min-w-0 gap-lg overflow-hidden rounded-lg bg-surface p-lg text-neutral raised-1 outline-none transition-colors focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `header`

- Element: `<div data-slot="audio-player-header">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex min-w-0 flex-wrap items-center gap-lg`
- `size`: small → `gap-md` · normal → `gap-lg` · large → `gap-xl`

## `artwork`

- Element: `<div data-slot="audio-player-artwork">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `relative grid shrink-0 place-items-center overflow-hidden rounded-sm border border-neutral-muted bg-neutral-muted text-neutral-muted-readable`
- `size`: small → `size-10` · normal → `size-12` · large → `size-14`

## `artworkImage`

- Element: `<img>`
- Base: `h-full w-full object-cover`

## `meta`

- Element: `<div data-slot="audio-player-meta">`
- Base: `min-w-0 flex-1`

## `title`

- Element: `<p data-slot="audio-player-title">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `truncate font-medium text-neutral`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `artist`

- Element: `<p data-slot="audio-player-artist">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `truncate text-neutral/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `controls`

- Element: `<div data-slot="audio-player-controls">`
- Variants: `size` (small | **normal** | large) · `layout` (**block** | inline) — bold is the default
- Base: `min-w-0 gap-md`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`
- `layout`: block → `grid flex-[1_1_100%] @lg:flex @lg:flex-[0_1_auto] @lg:flex-wrap @lg:items-center @lg:justify-end @lg:gap-xs` · inline → `flex shrink-0 flex-wrap items-center`

## `controlGroup`

- Element: `<div data-slot="audio-player-primary-controls">` / `<div data-slot="audio-player-secondary-controls">`
- Variants: `layout` (**block** | inline) — bold is the default
- Base: `flex min-w-0 flex-wrap items-center gap-xs`
- `layout`: block → `w-full @lg:w-auto` · inline → `w-auto shrink-0`

## `controlButton`

- Element: `<MediaIconButton>` / `<MediaVolumeControl>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `!border-neutral-muted data-[active=true]:!bg-selected/12 data-[active=true]:!text-selected-muted-readable`
- `size`: small → `!size-7 !min-w-7` · normal → `!size-8 !min-w-8` · large → `!size-9 !min-w-9`

## `playButton`

- Element: `<MediaIconButton>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: _none_
- `size`: small → `!size-8 !min-w-8` · normal → `!size-9 !min-w-9` · large → `!size-10 !min-w-10`

## `waveform`

- Element: `<div data-slot="audio-player-waveform">`
- Variants: `size` (small | **normal** | large) · `variant` (**centered** | histogram) · `disabled` (true | **false**) — bold is the default
- Base: `relative h-24 min-w-0 overflow-hidden rounded-sm border border-neutral-muted bg-neutral-muted/45 p-lg outline-none has-[input:focus-visible]:ring-2 has-[input:focus-visible]:ring-focus/50`
- `size`: small → `h-20 p-md` · normal → `h-24 p-lg` · large → `h-28 p-xl`
- `variant`: centered → _none_ · histogram → _none_
- `disabled`: true → `cursor-not-allowed opacity-50` · false → `cursor-pointer`

## `waveformBars`

- Element: `<div data-slot="audio-player-waveform-bars">`
- Variants: `variant` (**centered** | histogram) — bold is the default
- Base: `grid h-full min-w-0 gap-[3px]`
- `variant`: centered → `items-center` · histogram → `items-end`

## `waveformBar`

- Element: `<span data-slot="audio-player-waveform-bar">`
- Variants: `variant` (**centered** | histogram) — bold is the default
- Base: `relative min-h-2 min-w-0 overflow-hidden rounded-full bg-neutral/15`
- `variant`: centered → _none_ · histogram → _none_

## `waveformBarFill`

- Element: `<span data-slot="audio-player-waveform-bar-fill">`
- Base: `absolute inset-y-0 left-0 rounded-full bg-color`

## `waveformBarBuffered`

- Element: `<span data-slot="audio-player-waveform-bar-buffered">`
- Base: `absolute inset-y-0 left-0 rounded-full bg-neutral/25`

## `waveformInput`

- Element: `<input data-slot="audio-player-waveform-input">`
- Base: `absolute inset-0 z-10 m-0 h-full w-full cursor-pointer appearance-none bg-transparent opacity-0 focus:outline-none disabled:cursor-not-allowed`

## `track`

- Element: `<div data-slot="audio-player-track">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `relative min-w-0 rounded-full outline-none before:absolute before:inset-x-0 before:top-1/2 before:-translate-y-1/2 before:rounded-full before:bg-neutral/15 has-[input:focus-visible]:ring-2 has-[input:focus-visible]:ring-focus/50`
- `size`: small → `h-8 before:h-1.5` · normal → `h-10 before:h-2` · large → `h-12 before:h-2.5`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → `cursor-pointer`

## `trackBuffered`

- Element: `<span data-slot="audio-player-track-buffered">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `absolute left-0 top-1/2 -translate-y-1/2 rounded-full bg-neutral/25`
- `size`: small → `h-1.5` · normal → `h-2` · large → `h-2.5`

## `trackRange`

- Element: `<span data-slot="audio-player-track-range">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `absolute left-0 top-1/2 -translate-y-1/2 rounded-full bg-color`
- `size`: small → `h-1.5` · normal → `h-2` · large → `h-2.5`

## `trackInput`

- Element: `<input data-slot="audio-player-track-input">`
- Base: `absolute inset-0 z-10 m-0 h-full w-full cursor-pointer appearance-none bg-transparent opacity-0 focus:outline-none disabled:cursor-not-allowed`

## `inline`

- Element: `<div data-slot="audio-player-inline">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex min-w-0 flex-col gap-md @md:flex-row @md:items-center`
- `size`: small → `gap-sm` · normal → `gap-md` · large → `gap-lg`

## `inlineSeek`

- Element: `<div data-slot="audio-player-inline-seek">`
- Base: `min-w-0 flex-1`

## `inlineTrailing`

- Element: `<div data-slot="audio-player-inline-trailing">`
- Base: `shrink-0`

## `time`

- Element: `<span data-slot="audio-player-time">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `min-w-fit tabular-nums text-neutral/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `volumeControl`

- Element: `<MediaVolumeControl>`
- Variants: `layout` (**block** | inline) — bold is the default
- Base: `flex shrink-0 items-center`
- `layout`: block → _none_ · inline → _none_

## `popoverPanel`

- Element: `<MediaVolumeControl>`
- Base: _none_

## `volumePanel`

- Element: `<MediaVolumeControl>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex min-w-0 flex-col items-center gap-md p-xs`
- `size`: small → `gap-md` · normal → `gap-lg` · large → `gap-xl`

## `volumeSlider`

- Element: `<MediaVolumeControl>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex shrink-0 items-center justify-center`
- `size`: small → `h-32` · normal → `h-36` · large → `h-40`

## `status`

- Element: `<p data-slot="audio-player-status">`
- Variants: `size` (small | **normal** | large) · `tone` (**loading** | error) — bold is the default
- Base: `rounded-sm border border-neutral-muted bg-surface px-md py-xs text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `tone`: loading → _none_ · error → `border-danger/40 text-danger-readable`
