# dialog theme parts

Generated by `npm run generate:component-contract` from `Dialog`'s theme file. Do not edit.

- Registry key: `dialog`, the key under `<Theme components>`
- Setter: `setDialogTheme` from `entasis/dialog`
- Parts: 11. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: { x: 0, y: 0, scale: 0.98, opacity: 0 }, out: { x: 0, y: 0, scale: 0.98, opacity: 0 } }`
- `type`: modal → `{}` · fullScreen → `{}` · drawerRight → `{ in: { x: '100%' }, out: { x: '100%' } }` · drawerLeft → `{ in: { x: '-100%' }, out: { x: '-100%' } }` · drawerBottom → `{ in: { y: '100%' }, out: { y: '100%' } }` · drawerTop → `{ in: { y: '-100%' }, out: { y: '-100%' } }` · alert → `{ in: { y: -100 }, out: { y: -100 } }`

## `root`

- Element: `<div>`
- Variants: `scroll` (**inner** | outer) — bold is the default
- Base: `fixed inset-0`
- `scroll`: inner → `overflow-hidden` · outer → `overflow-y-auto`

## `align`

- Element: `<div>`
- Variants: `type` (fullScreen | drawerRight | drawerLeft | drawerBottom | drawerTop | modal | alert) · `scroll` (**inner** | outer) — bold is the default
- Base: `flex p-layout-md`
- `type`: fullScreen → `justify-center items-center` · drawerRight → `justify-end p-(--drawer-inset)` · drawerLeft → `justify-start p-(--drawer-inset)` · drawerBottom → `justify-center items-end p-(--drawer-inset)` · drawerTop → `justify-center items-start p-(--drawer-inset)` · modal → `justify-center items-center` · alert → `justify-center items-center`
- `scroll`: inner → `h-full` · outer → `min-h-full`

## `backdrop`

- Element: `<div>`
- Base: `fixed inset-0 bg-neutral/40 backdrop-blur-xs`

## `content`

- Element: `<div>`
- Variants: `size` (small | normal | large) · `type` (fullScreen | drawerRight | drawerLeft | drawerBottom | drawerTop | modal | alert) · `scroll` (**inner** | outer) — bold is the default
- Base: `z-10 relative px-xl py-md raised-xl h-fit bg-surface-floating text-neutral rounded-xl flex flex-col z-50 will-change-transform transition-transform duration-normal ease-standard`
- `size`: small → `max-w-md w-full` · normal → `max-w-xl w-full` · large → `max-w-3xl w-full`
- `type`: fullScreen → `h-full w-full max-w-full origin-center` · drawerRight → `rounded-r-[var(--drawer-edge-radius)] h-full origin-right` · drawerLeft → `rounded-l-[var(--drawer-edge-radius)] h-full origin-left` · drawerBottom → `rounded-b-[var(--drawer-edge-radius)] max-w-full origin-bottom` · drawerTop → `rounded-t-[var(--drawer-edge-radius)] max-w-full origin-top` · modal → `origin-center` · alert → `origin-center`
- `scroll`: inner → `max-h-full overflow-auto overscroll-none` · outer → _none_

## `thumb`

- Element: `<div>`
- Variants: `type` (fullScreen | drawerRight | drawerLeft | drawerBottom | drawerTop | modal | alert) — bold is the default
- Base: `absolute z-10 touch-none rounded-full bg-neutral-muted before:absolute before:-inset-4 before:content-['']`
- `type`: fullScreen → `hidden` · drawerRight → `left-1.5 top-1/2 -translate-y-1/2 h-12 w-1.5` · drawerLeft → `right-1.5 top-1/2 -translate-y-1/2 h-12 w-1.5` · drawerBottom → `left-1/2 -translate-x-1/2 top-1.5 h-1.5 w-12` · drawerTop → `left-1/2 -translate-x-1/2 bottom-1.5 h-1.5 w-12` · modal → `hidden` · alert → `hidden`

## `header`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `grid gap-xs mb-md border-b border-neutral-muted py-md`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `footer`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: _none_
- `size`: small → _none_ · normal → _none_ · large → _none_

## `closeButton`

- Element: `<Slot>` / `<Button>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `state-layer ml-auto rounded-full absolute top-1 right-1 p-xs text-neutral`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `title`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-lg font-semibold text-neutral`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `description`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-sm text-neutral/70`
- `size`: small → _none_ · normal → _none_ · large → _none_
