# media-volume-control theme parts

Generated by `npm run generate:component-contract` from `MediaVolumeControl`'s theme file. Do not edit.

- Registry key: `media-volume-control`, the key under `<Theme components>`
- Setter: `setMediaVolumeControlTheme` from `entasis/media-volume`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="media-volume-control">`
- Variants: `mode` (**popover** | inline) — bold is the default
- Base: `flex min-w-0 shrink-0 items-center`
- `mode`: popover → _none_ · inline → `w-full`

## `popoverPanel`

- Element: `<Popover>`
- Base: `p-sm pb-md`

## `panel`

- Element: `<div data-slot="media-volume-panel">`
- Variants: `orientation` (**horizontal** | vertical) — bold is the default
- Base: `flex min-w-0 items-center gap-md p-xs`
- `orientation`: horizontal → `w-full` · vertical → `w-auto flex-col justify-center gap-sm px-0 pt-micro pb-0`

## `slider`

- Element: `<div data-slot="media-volume-slider">`
- Variants: `orientation` (**horizontal** | vertical) — bold is the default
- Base: `flex min-w-0 shrink-0 items-center justify-center`
- `orientation`: horizontal → `w-full` · vertical → `h-auto`
