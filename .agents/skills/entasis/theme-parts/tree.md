# tree theme parts

Generated by `npm run generate:component-contract` from `Tree`'s theme file. Do not edit.

- Registry key: `tree`, the key under `<Theme components>`
- Setter: `setTreeTheme` from `entasis/tree`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="tree">`
- Base: `my-xl flex w-full min-w-0 flex-col overflow-hidden rounded-lg border border-neutral-muted bg-surface text-sm text-neutral`

## `viewport`

- Element: `<div>`
- Base: `min-h-0 min-w-0 flex-1 overflow-hidden`

## `host`

- Element: not applied in this component's markup
- Base: `block h-full min-h-0 w-full min-w-0`

## `error`

- Element: `<div>`
- Base: `border-b border-danger/40 bg-danger-muted px-xl py-lg text-sm text-danger-muted-readable`
