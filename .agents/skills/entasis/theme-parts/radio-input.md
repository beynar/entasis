# radio-input theme parts

Generated by `npm run generate:component-contract` from `RadioInput`'s theme file. Do not edit.

- Registry key: `radio-input`, the key under `<Theme components>`
- Setter: `setRadioInputTheme` from `entasis/radio-input`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<Field>`
- Variants: `mode` (card | normal) — bold is the default
- Base: _none_
- `mode`: card → _none_ · normal → _none_

## `radiosInputItem`

- Element: `<button>`
- Variants: `mode` (card | normal) · `size` (small | **normal** | large) · `checked` (true | false) · `disabled` (true | false) — bold is the default
- Base: `relative grid w-full cursor-pointer items-start gap-xs rounded-md text-left transition-[color,background-color,box-shadow,opacity] outline-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `mode`: card → `raised bg-surface-raised py-md pl-layout-lg` · normal → `py-xs pl-layout-xl`
- `size`: small → `min-h-row-sm` · normal → `min-h-row-md` · large → `min-h-row-lg`
- `checked`: true → _none_ · false → _none_
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
- Compound: mode=card & checked=true → `ring-2 ring-selected bg-selected-muted text-selected-muted-readable`

## `radiosInputItemLabel`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex items-center gap-xl flex-wrap`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `radiosInputItemTrack`

- Element: `<div>`
- Variants: `checked` (true | false) · `size` (small | normal | large) · `mode` (card | normal) · `disabled` (true | false) — bold is the default
- Base: `origin-center content-[""] bg-surface-raised border border-neutral-muted rounded-full absolute left-2`
- `checked`: true → _none_ · false → _none_
- `size`: small → `size-4` · normal → `size-5` · large → `size-6`
- `mode`: card → `top-3` · normal → `top-1`
- `disabled`: true → _none_ · false → _none_
- Compound: mode=card & size=small → `size-3.5` · mode=card & size=normal → `size-4` · mode=card & size=large → `size-5`

## `radiosInputItemThumb`

- Element: `<div>`
- Variants: `checked` (true | false) · `size` (small | normal | large) · `mode` (card | normal) · `disabled` (true | false) — bold is the default
- Base: `origin-center radio rounded-full transition-[background-color,opacity,scale] content-[""] absolute left-2 scale-[40%] opacity-0`
- `checked`: true → `bg-selected scale-[60%] opacity-100` · false → _none_
- `size`: small → `size-4` · normal → `size-5` · large → `size-6`
- `mode`: card → `top-3` · normal → `top-1`
- `disabled`: true → _none_ · false → _none_
- Compound: mode=card & size=small → `size-3.5` · mode=card & size=normal → `size-4` · mode=card & size=large → `size-5`

## `radiosInputItemIcon`

- Element: `<Slot>`
- Base: _none_

## `radiosInputItemDescription`

- Element: `<Slot>`
- Variants: `mode` (card | normal) · `checked` (true | false) — bold is the default
- Base: `text-xs text-neutral/70`
- `mode`: card → _none_ · normal → _none_
- `checked`: true → _none_ · false → _none_
- Compound: mode=card & checked=true → `text-selected-muted-readable`

## `radiosInputContainer`

- Element: not applied in this component's markup
- Variants: `mode` (card | normal) · `disabled` (true | false) — bold is the default
- Base: `grid gap-lg grid-cols-1 items-start`
- `mode`: card → _none_ · normal → _none_
- `disabled`: true → _none_ · false → _none_
