# pin-input theme parts

Generated by `npm run generate:component-contract` from `PinInput`'s theme file. Do not edit.

- Registry key: `pin-input`, the key under `<Theme components>`
- Setter: `setPinInputTheme` from `entasis/pin-input`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `inputContainer`

- Element: `<Field>`
- Variants: `disabled` (true | false) — bold is the default
- Base: `relative w-fit bg-transparent p-0 text-neutral`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `root`

- Element: `<div data-slot="pin-input">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `relative inline-flex items-center`
- `size`: small → `gap-xs` · normal → `gap-sm` · large → `gap-md`
- `disabled`: true → `cursor-not-allowed` · false → `cursor-text`

## `input`

- Element: `<input data-slot="pin-input-input">`
- Base: `absolute inset-0 z-10 size-full appearance-none border-0 bg-transparent text-transparent caret-transparent outline-none selection:bg-transparent selection:text-transparent [font-size:16px] disabled:cursor-not-allowed`

## `cell`

- Element: `<div data-slot="pin-input-cell">`
- Variants: `size` (small | **normal** | large) · `active` (true | **false**) · `disabled` (true | **false**) — bold is the default
- Base: `relative grid shrink-0 place-items-center overflow-hidden raised-1 bg-surface-raised font-mono font-medium tabular-nums text-neutral transition-[color,background-color,border-color,box-shadow]`
- `size`: small → `size-8 rounded-md text-xs` · normal → `size-10 rounded-md text-sm` · large → `size-12 rounded-md text-sm`
- `active`: true → `[--raised-border:1px_solid_var(--color-neutral)] ring-2 ring-selected/40` · false → _none_
- `disabled`: true → `opacity-60` · false → _none_

## `character`

- Element: `<span data-slot="pin-input-character">`
- Base: `select-none leading-none`

## `caret`

- Element: `<span data-slot="pin-input-caret">`
- Base: `absolute h-5 w-px animate-pulse rounded-full bg-primary`
