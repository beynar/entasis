# metadata-list theme parts

Generated by `npm run generate:component-contract` from `MetadataList`'s theme file. Do not edit.

- Registry key: `metadata-list`, the key under `<Theme components>`
- Setter: `setMetadataListTheme` from `entasis/metadata-list`
- Parts: 13. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex flex-col`
- `density`: compact → `gap-md` · normal → `gap-lg` · comfortable → `gap-xl`

## `header`

- Element: `<div>`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex flex-col`
- `density`: compact → `gap-micro` · normal → `gap-xs` · comfortable → `gap-xs`

## `title`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `font-medium text-neutral`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `list`

- Element: `<dl>` / `<div>`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: `grid`
- `density`: compact → `gap-x-xl gap-y-md` · normal → `gap-x-layout-md gap-y-md` · comfortable → `gap-x-layout-lg gap-y-lg`

## `item`

- Element: `<div>`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex items-start`
- `density`: compact → `gap-md` · normal → `gap-lg` · comfortable → `gap-xl`

## `key`

- Element: `<dt>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex items-center w-[35%] min-w-24 shrink-0 text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `density`: compact → `gap-sm` · normal → `gap-md` · comfortable → `gap-md`

## `keyIcon`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `shrink-0 [&>svg]:size-full`
- `size`: small → `size-3.5` · normal → `size-4` · large → `size-5`

## `value`

- Element: `<dd>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex-1 min-w-0 text-neutral`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `link`

- Element: `<a>`
- Base: `underline decoration-neutral-muted/40 underline-offset-2 hover:decoration-current transition-colors`

## `chips`

- Element: `<div>`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex flex-wrap`
- `density`: compact → `gap-xs` · normal → `gap-sm` · comfortable → `gap-md`

## `toggle`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) — bold is the default
- Base: `inline-flex items-center self-start cursor-pointer rounded-sm text-neutral/70 outline-none transition-colors hover:text-neutral focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `density`: compact → `gap-xs` · normal → `gap-sm` · comfortable → `gap-md`

## `toggleIcon`

- Element: `<span>`
- Variants: `expanded` (true | **false**) — bold is the default
- Base: `inline-flex transition-transform duration-normal`
- `expanded`: true → `rotate-180` · false → _none_
