# spinner-text theme parts

Generated by `npm run generate:component-contract` from `SpinnerText`'s theme file. Do not edit.

- Registry key: `spinner-text`, the key under `<Theme components>`
- Setter: `setSpinnerTextTheme` from `entasis/spinner-text`
- Parts: 7. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: {}, out: {}, duration: 'slow', easing: 'standard' }`
- `mode`: vertical → `{}` · reveal → `{ duration: 'slower' }`

## `root`

- Element: `<span data-slot="spinner-text">`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | danger | success | warning | info | **neutral**) — bold is the default
- Base: `inline-flex min-w-0 items-center align-middle`
- `size`: small → `gap-xs` · normal → `gap-sm` · large → `gap-md`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable` · neutral → `text-neutral`

## `spinner`

- Element: `<Slot>` / `<Spinner>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `inline-flex shrink-0 items-center justify-center`
- `size`: small → `[&_[data-slot=spinner-indicator]]:[--spinner-size:0.75rem]` · normal → `[&_[data-slot=spinner-indicator]]:[--spinner-size:1rem]` · large → `[&_[data-slot=spinner-indicator]]:[--spinner-size:1.25rem]`

## `viewport`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `relative isolate grid min-w-0 overflow-hidden`
- `size`: small → `h-4 text-xs leading-4` · normal → `h-5 text-sm leading-5` · large → `h-6 text-base leading-6`

## `sizer`

- Element: `<span>`
- Base: `invisible col-start-1 row-start-1 grid pointer-events-none select-none`

## `sizerItem`

- Element: `<span>`
- Base: `col-start-1 row-start-1 whitespace-nowrap`

## `message`

- Element: `<span>`
- Variants: `shimmer` (true | **false**) — bold is the default
- Base: `col-start-1 row-start-1 whitespace-nowrap will-change-[transform,opacity,clip-path]`
- `shimmer`: true → `shimmer` · false → _none_
