# link-preview theme parts

Generated by `npm run generate:component-contract` from `LinkPreview`'s theme file. Do not edit.

- Registry key: `link-preview`, the key under `<Theme components>`
- Setter: `setLinkPreviewTheme` from `entasis/link-preview`
- Parts: 14. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `trigger`

- Element: `<a>`
- Variants: `disabled` (true | **false**) — bold is the default
- Base: `text-primary-readable inline-flex max-w-full items-center rounded-sm underline-offset-4 transition-colors hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `disabled`: true → `pointer-events-none opacity-55` · false → _none_

## `card`

- Element: `<HoverCard>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `max-w-[calc(100vw-2rem)] overflow-hidden gap-0! py-0!`
- `size`: small → `w-64` · normal → `w-80` · large → `w-96`

## `content`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `grid min-w-0 gap-0`
- `size`: small → `-mx-md` · normal → `-mx-xl` · large → `-mx-layout-md`

## `media`

- Element: `<div>` / `<Skeleton>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `bg-neutral-muted overflow-hidden rounded-t-lg rounded-b-none`
- `size`: small → `aspect-[1.9/1]` · normal → `aspect-[1.9/1]` · large → `aspect-[1.9/1]`

## `image`

- Element: `<img>`
- Base: `h-full w-full object-cover`

## `body`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `grid min-w-0 gap-md`
- `size`: small → `px-md pt-lg pb-lg` · normal → `px-xl pt-xl pb-xl` · large → `px-layout-md pt-layout-sm pb-layout-sm`

## `header`

- Element: `<div>`
- Base: `flex min-w-0 items-center gap-md`

## `favicon`

- Element: `<img>`
- Base: `size-4 shrink-0 rounded-sm`

## `site`

- Element: `<span>`
- Base: `text-neutral/70 min-w-0 truncate text-xs font-medium`

## `title`

- Element: `<p>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral line-clamp-2 font-semibold leading-snug`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<p>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 line-clamp-3 leading-relaxed`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `url`

- Element: `<p>`
- Base: `text-neutral/70 truncate font-mono text-xs`

## `loading`

- Element: `<div>`
- Base: `grid gap-0`

## `error`

- Element: `<div>`
- Base: `border-danger/20 bg-danger-muted/20 text-danger-muted-readable rounded-lg border p-lg`
