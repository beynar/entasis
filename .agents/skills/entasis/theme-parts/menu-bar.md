# menu-bar theme parts

Generated by `npm run generate:component-contract` from `MenuBar`'s theme file. Do not edit.

- Registry key: `menu-bar`, the key under `<Theme components>`
- Setter: `setMenuBarTheme` from `entasis/menu-bar`
- Parts: 2. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `bg-surface inline-flex w-fit max-w-full items-center overflow-x-auto rounded-md scrollbar-none`
- `size`: small → `gap-micro p-micro` · normal → `gap-micro p-xs` · large → `gap-xs p-xs`

## `trigger`

- Element: `<Button>`
- Variants: `active` (true | **false**) — bold is the default
- Base: `focus-visible:ring-inset`
- `active`: true → _none_ · false → _none_
