# rating theme parts

Generated by `npm run generate:component-contract` from `Rating`'s theme file. Do not edit.

- Registry key: `rating`, the key under `<Theme components>`
- Setter: `setRatingTheme` from `entasis/rating`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `container`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `inline-flex items-center rounded-sm outline-none focus-visible:ring-2 focus-visible:ring-focus/50 transition-[box-shadow,opacity]`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `star`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) · `interactive` (true | **false**) — bold is the default
- Base: `relative inline-flex shrink-0 items-center justify-center`
- `size`: small → `size-5` · normal → `size-6` · large → `size-7`
- `interactive`: true → `cursor-pointer` · false → _none_

## `starBase`

- Element: `<span>`
- Base: `absolute inset-0 text-neutral/45`

## `starFill`

- Element: `<span>`
- Variants: `color` (primary | secondary | neutral | danger | success | **warning** | info) — bold is the default
- Base: `absolute inset-y-0 overflow-hidden`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · neutral → `text-neutral` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable`
