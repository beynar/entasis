# phone-input theme parts

Generated by `npm run generate:component-contract` from `PhoneInput`'s theme file. Do not edit.

- Registry key: `phone-input`, the key under `<Theme components>`
- Setter: `setPhoneInputTheme` from `entasis/phone-input`
- Parts: 18. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `outline-none flex-1 w-full min-w-0 h-full bg-transparent resize-none placeholder:text-neutral/70 autofill:text-neutral appearance-none text-sm leading-normal`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `flex w-full items-center border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `h-control-sm text-xs` · normal → `h-control-md text-sm` · large → `h-control-lg text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `countryTrigger`

- Element: `<Button>`
- Variants: `size` (small | **normal** | large) · `open` (true | **false**) — bold is the default
- Base: `-ml-lg mr-md inline-flex h-auto shrink-0 self-stretch rounded-none border-0 bg-clip-border px-lg text-neutral focus-visible:ring-0 focus-visible:text-primary-readable active:translate-y-0 active:bg-transparent`
- `size`: small → `gap-xs text-xs` · normal → `gap-sm text-sm` · large → `gap-sm text-sm`
- `open`: true → `text-primary-readable` · false → _none_

## `countryFlag`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `inline-flex shrink-0 items-center justify-center leading-none`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`

## `countryDialCode`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `font-medium tabular-nums`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`

## `countryChevron`

- Element: not applied in this component's markup
- Variants: `open` (true | **false**) — bold is the default
- Base: `text-neutral/70 size-3 shrink-0 transition-transform`
- `open`: true → `rotate-180 text-primary-readable` · false → _none_

## `popover`

- Element: `<Popover>`
- Base: `w-[22rem] max-w-[calc(100vw-2rem)] p-0`

## `countryPicker`

- Element: `<div>`
- Base: `flex max-h-[calc(100vh-8rem)] flex-col gap-md overflow-hidden p-md`

## `countrySearch`

- Element: `<TextInput>`
- Base: `w-full`

## `countrySearchInputContainer`

- Element: `<TextInput>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `bg-surface-canvas`
- `size`: small → `py-xs` · normal → `py-xs` · large → `py-sm`

## `countrySearchInput`

- Element: `<TextInput>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `placeholder:text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`

## `countryList`

- Element: `<ScrollArea>`
- Base: `h-72 max-h-[calc(100vh-14rem)] min-h-0 overflow-hidden`

## `countryListbox`

- Element: `<div>`
- Base: `flex min-w-full flex-col gap-xs`

## `countryOption`

- Element: `<Button>`
- Variants: `size` (small | **normal** | large) · `highlighted` (true | **false**) · `selected` (true | **false**) — bold is the default
- Base: `w-full justify-start gap-md rounded-sm border-0 bg-clip-border px-md text-left font-normal shadow-none outline-none focus-visible:ring-0 active:translate-y-0`
- `size`: small → `min-h-row-sm py-xs text-xs` · normal → `min-h-row-sm py-xs text-sm` · large → `min-h-row-md py-sm text-sm`
- `highlighted`: true → _none_ · false → _none_
- `selected`: true → _none_ · false → `text-neutral`

## `countryName`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `min-w-0 flex-1 truncate`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`

## `countryOptionDialCode`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 ml-auto tabular-nums`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`

## `countryCheck`

- Element: not applied in this component's markup
- Base: `size-4 shrink-0`

## `countryEmpty`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 px-md py-layout-md text-center`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
