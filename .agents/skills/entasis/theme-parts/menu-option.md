# menu-option theme parts

Generated by `npm run generate:component-contract` from `MenuOption`'s theme file. Do not edit.

- Registry key: `menu-option`, the key under `<Theme components>`
- Setter: `setMenuOptionTheme` from `entasis/menu-option`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<svelte:element>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) · `color` (**primary** | secondary | neutral | danger | success | warning | info) · `disabled` (true | **false**) · `highlighted` (true | false) · `active` (true | **false**) — bold is the default
- Base: `state-layer rounded-md-concentric cursor-pointer items-center inline-flex relative w-full text-left outline-none`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `density`: compact → `px-sm py-xs gap-sm min-h-6` · normal → `px-md py-sm gap-md min-h-row-sm` · comfortable → `px-lg py-md gap-md min-h-row-md`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · neutral → `text-neutral` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable`
- `disabled`: true → `cursor-not-allowed opacity-50 pointer-events-none` · false → _none_
- `highlighted`: true → _none_ · false → _none_
- `active`: true → `bg-selected-muted text-selected-muted-readable` · false → _none_

## `title`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `font-normal leading-none`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70  leading-none`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-xs`

## `prefix`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `align` (start | center) — bold is the default
- Base: `flex shrink-0 items-center justify-center`
- `size`: small → `size-3.5 [&_svg]:size-icon-sm` · normal → `size-4 [&_svg]:size-icon-md` · large → `size-5 [&_svg]:size-icon-lg`
- `align`: start → `mb-auto` · center → `my-auto`

## `suffix`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `ml-auto flex shrink-0 items-center`
- `size`: small → `[&_svg]:size-icon-xs` · normal → `[&_svg]:size-icon-md` · large → `[&_svg]:size-icon-lg`

## `content`

- Element: `<div>`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex flex-col flex-1`
- `density`: compact → `gap-0` · normal → `gap-micro` · comfortable → `gap-xs`
