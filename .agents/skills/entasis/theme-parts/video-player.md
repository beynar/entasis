# video-player theme parts

Generated by `npm run generate:component-contract` from `VideoPlayer`'s theme file. Do not edit.

- Registry key: `video-player`, the key under `<Theme components>`
- Setter: `setVideoPlayerTheme` from `entasis/video-player`
- Parts: 28. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="video-player">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) · `fullscreen` (fullscreen | **windowed**) — bold is the default
- Base: `group/video-player @container relative isolate w-full min-w-0 overflow-hidden rounded-xl border border-neutral-muted bg-black text-white outline-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
- `fullscreen`: fullscreen → `h-full w-full rounded-none border-0` · windowed → _none_

## `aspectRatio`

- Element: `<AspectRatio>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `w-full bg-black`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `frame`

- Element: `<div data-slot="video-player-frame">`
- Variants: `ratio` (**auto** | fixed | fullscreen) — bold is the default
- Base: `relative min-h-40 w-full bg-black`
- `ratio`: auto → _none_ · fixed → `absolute inset-0 min-h-0` · fullscreen → `h-full min-h-0`

## `media`

- Element: `<video data-slot="video-player-media">`
- Variants: `disabled` (true | **false**) — bold is the default
- Base: `block h-full w-full bg-black object-contain`
- `disabled`: true → `pointer-events-none` · false → _none_

## `overlay`

- Element: `<SlotComponent>`
- Variants: `visible` (**true** | false) — bold is the default
- Base: `pointer-events-none absolute inset-0 z-10 flex items-center justify-center p-xl`
- `visible`: true → _none_ · false → `hidden`

## `panel`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `tone` (**loading** | error | empty) — bold is the default
- Base: `rounded-lg border border-white/15 bg-black/70 px-lg py-md text-center text-white lift-4 backdrop-blur`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `tone`: loading → _none_ · error → `border-danger/60 text-danger-lighter` · empty → `text-white/70`

## `controls`

- Element: `<div data-slot="video-player-controls">`
- Variants: `visible` (**true** | false) · `size` (small | **normal** | large) — bold is the default
- Base: `absolute inset-0 z-20 text-white transition-opacity duration-normal`
- `visible`: true → `opacity-100` · false → `pointer-events-none opacity-0`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `controlsBackdrop`

- Element: `<div>`
- Base: `pointer-events-none absolute inset-x-0 bottom-0 z-10 h-24 bg-linear-to-t from-black/85 via-black/45 to-transparent @md:h-32`

## `controlsBar`

- Element: `<div data-slot="video-player-controls-bar">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `absolute inset-x-0 bottom-0 z-20 grid gap-md @max-md:flex @max-md:items-center @max-md:gap-sm`
- `size`: small → `gap-sm p-xs @md:p-md` · normal → `gap-md p-xs @md:p-md @xl:p-lg` · large → `gap-md p-md @md:p-lg @xl:p-xl`

## `controlRow`

- Element: `<div data-slot="video-player-control-row">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `grid min-w-0 grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)] items-center gap-md @max-md:contents @md:flex @md:flex-nowrap`
- `size`: small → `gap-xs @md:gap-xs` · normal → `gap-sm @md:gap-xs @xl:gap-sm` · large → `gap-md @md:gap-sm @xl:gap-md`

## `controlStart`

- Element: `<div data-slot="video-player-control-start">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `order-1 flex min-w-0 items-center justify-start @max-md:hidden @md:order-2`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `controlCenter`

- Element: `<div data-slot="video-player-control-center">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `order-2 flex shrink-0 items-center justify-center gap-xs @max-md:hidden @md:order-1 @md:justify-start`
- `size`: small → `gap-micro @md:gap-xs` · normal → `gap-xs @md:gap-xs @xl:gap-sm` · large → `gap-sm @md:gap-sm @xl:gap-md`

## `controlEnd`

- Element: `<div data-slot="video-player-control-end">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `order-3 flex min-w-0 items-center justify-end gap-micro @max-md:min-w-fit @max-md:flex-none @md:ml-auto`
- `size`: small → `gap-micro @md:gap-xs` · normal → `gap-micro @md:gap-xs @xl:gap-sm` · large → `gap-xs @md:gap-sm @xl:gap-md`

## `controlButton`

- Element: `<MediaIconButton>` / `<MediaVolumeControl>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `!border-white/10 !text-white hover:!bg-white/15 active:!bg-white/20 data-[active=true]:!bg-white/20`
- `size`: small → `!size-6 !min-w-6 @md:!size-7 @md:!min-w-7` · normal → `!size-6 !min-w-6 @md:!size-7 @md:!min-w-7 @xl:!size-8 @xl:!min-w-8` · large → `!size-7 !min-w-7 @md:!size-8 @md:!min-w-8 @xl:!size-9 @xl:!min-w-9`

## `compactTransport`

- Element: `<div data-slot="video-player-compact-transport">`
- Base: `absolute top-1/2 left-1/2 z-30 hidden -translate-x-1/2 -translate-y-1/2 items-center gap-sm @max-md:flex`

## `compactSettings`

- Element: `<div data-slot="video-player-compact-settings">`
- Base: `hidden @max-md:flex`

## `secondaryControls`

- Element: `<div data-slot="video-player-secondary-controls">`
- Base: `contents @max-md:hidden`

## `slider`

- Element: `<MediaVolumeControl>` / `<div data-slot="video-player-slider">` / `<div data-slot="video-player-timeline-slider">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `relative flex w-full min-w-0 flex-1 items-center gap-md py-sm`
- `size`: small → `py-xs` · normal → `py-sm` · large → `py-md`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `timelineTrack`

- Element: `<div data-slot="video-player-timeline-track">`
- Variants: `disabled` (true | **false**) — bold is the default
- Base: `group relative h-11 w-full min-w-0 touch-none rounded-full outline-none has-[input:focus-visible]:ring-2 has-[input:focus-visible]:ring-white/60 has-[input:focus-visible]:ring-offset-0`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → `cursor-pointer`

## `timelineRail`

- Element: `<div data-slot="video-player-timeline-rail">`
- Base: `absolute inset-x-0 bottom-0 h-1.5 overflow-hidden rounded-full bg-white/25 @max-md:top-1/2 @max-md:bottom-auto @max-md:-translate-y-1/2`

## `timelineBuffered`

- Element: `<div data-slot="video-player-timeline-buffered">`
- Base: `absolute inset-y-0 left-0 rounded-full bg-white/35`

## `timelineRange`

- Element: `<div data-slot="video-player-timeline-range">`
- Base: `absolute inset-y-0 left-0 rounded-full bg-primary`

## `timelineThumb`

- Element: `<div data-slot="video-player-timeline-thumb">`
- Base: `pointer-events-none absolute bottom-[-3px] z-10 size-3 -translate-x-1/2 rounded-full bg-white shadow transition-transform group-focus-within:scale-110 @max-md:top-1/2 @max-md:bottom-auto @max-md:-translate-y-1/2`

## `timelineInput`

- Element: `<input data-slot="video-player-timeline-input">`
- Base: `absolute inset-x-0 top-1/2 z-20 m-0 h-11 w-full -translate-y-1/2 cursor-pointer appearance-none bg-transparent opacity-0 focus:outline-none disabled:cursor-not-allowed`

## `time`

- Element: `<span data-slot="video-player-time">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `min-w-fit tabular-nums text-white`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `menu`

- Element: `<PopupMenu>`
- Base: `grid gap-xs`

## `popoverPanel`

- Element: `<PopupMenu>` / `<MediaVolumeControl>`
- Base: `border-white/10 bg-black/90 text-white lift-5 backdrop-blur`

## `volumePanel`

- Element: `<MediaVolumeControl>`
- Base: `flex w-full min-w-0 items-center gap-md p-xs`
