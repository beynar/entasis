# multi-step-form theme parts

Generated by `npm run generate:component-contract` from `MultiStepForm`'s theme file. Do not edit.

- Registry key: `multi-step-form`, the key under `<Theme components>`
- Setter: `setMultiStepFormTheme` from `entasis/multi-step-form`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="multi-step-form">`
- Variants: `variant` (**plain** | sectioned | card) — bold is the default
- Base: `relative flex min-w-0 flex-col`
- `variant`: plain → `gap-xl` · sectioned → `gap-xl` · card → _none_

## `multiStepFormHeader`

- Element: `<Slot>`
- Variants: `variant` (**plain** | sectioned | card) — bold is the default
- Base: `px-xl`
- `variant`: plain → _none_ · sectioned → _none_ · card → `border-b border-neutral-muted pb-xl`

## `multiStepFormContent`

- Element: `<div>`
- Base: `min-w-0 px-xl`

## `multiStepFormFooter`

- Element: `<Slot>`
- Variants: `variant` (**plain** | sectioned | card) — bold is the default
- Base: `flex justify-between gap-md px-xl`
- `variant`: plain → _none_ · sectioned → _none_ · card → `border-t border-neutral-muted pt-xl`
