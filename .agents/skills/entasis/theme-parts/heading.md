# heading theme parts

Generated by `npm run generate:component-contract` from `Heading`'s theme file. Do not edit.

- Registry key: `heading`, the key under `<Theme components>`
- Setter: `setHeadingTheme` from `entasis/heading`
- Parts: 1. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<svelte:element>`
- Variants: `size` (h1 | **h2** | h3 | h4 | h5 | h6) · `weight` (**normal** | bold | light) · `align` (**left** | center | right) · `balanced` (**true** | false) · `underline` (true | **false**) · `muted` (true | **false**) · `trim` (start | end | **both** | none) — bold is the default
- Base: `[text-box-edge:cap_alphabetic]`
- `size`: h1 → `text-4xl` · h2 → `text-3xl` · h3 → `text-2xl` · h4 → `text-xl` · h5 → `text-lg` · h6 → `text-base`
- `weight`: normal → `font-normal` · bold → `font-bold` · light → `font-light`
- `align`: left → `text-left` · center → `text-center` · right → `text-right`
- `balanced`: true → `text-balance` · false → `text-wrap`
- `underline`: true → `underline` · false → _none_
- `muted`: true → `text-neutral/70` · false → _none_
- `trim`: start → `[text-box-trim:trim-start]` · end → `[text-box-trim:trim-end]` · both → `[text-box-trim:trim-both]` · none → `[text-box-trim:none]`
