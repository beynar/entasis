# alert theme parts

Generated by `npm run generate:component-contract` from `Alert`'s theme file. Do not edit.

- Registry key: `alert`, the key under `<Theme components>`
- Setter: `setAlertTheme` from `entasis/alert`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `hasIcon` (true | **false**) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `variant` (solid | **outline** | soft) · `size` (small | **normal** | large) · `disabled` (true | **false**) · `hasDescription` (true | false) · `hasTitle` (true | false) — bold is the default
- Base: `relative w-full rounded-lg border px-xl py-lg text-sm flex gap-lg items-start`
- `hasIcon`: true → _none_ · false → _none_
- `color`: primary → `bg-primary text-primary-contrast border-primary` · secondary → `bg-secondary text-secondary-contrast border-secondary` · neutral → `bg-neutral text-neutral-contrast border-neutral` · danger → `bg-danger text-danger-contrast border-danger` · success → `bg-success text-success-contrast border-success` · warning → `bg-warning text-warning-contrast border-warning` · info → `bg-info text-info-contrast border-info`
- `variant`: solid → `bg-color text-color-contrast border-color` · outline → `bg-surface-raised border-neutral-muted text-neutral` · soft → `bg-color-muted text-color-muted-readable border-color/20`
- `size`: small → `px-lg py-md text-xs` · normal → `px-xl py-lg text-sm` · large → `px-xl py-xl text-base`
- `disabled`: true → `opacity-50 cursor-not-allowed pointer-events-none` · false → _none_
- `hasDescription`: true → _none_ · false → _none_
- `hasTitle`: true → _none_ · false → _none_
- Compound: hasDescription=false & hasTitle=true & hasIcon=true → `items-center`

## `prefix`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `variant` (solid | **outline** | soft) · `hasDescription` (true | **false**) — bold is the default
- Base: `shrink-0 [&>svg]:text-current`
- `size`: small → `[&>svg]:size-icon-md ` · normal → `[&>svg]:size-icon-lg ` · large → `[&>svg]:size-icon-xl `
- `variant`: solid → _none_ · outline → `[&>svg]:text-color-readable` · soft → `[&>svg]:text-color-readable`
- `hasDescription`: true → `translate-y-0.5` · false → _none_

## `content`

- Element: `<div>`
- Base: `flex min-w-0 flex-1 flex-col gap-micro`

## `title`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `line-clamp-2 font-medium`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `variant` (solid | **outline** | soft) — bold is the default
- Base: `flex flex-col gap-xs text-sm [&_p]:leading-relaxed`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `variant`: solid → _none_ · outline → `text-neutral/70` · soft → _none_

## `close`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `variant` (solid | **outline** | soft) — bold is the default
- Base: `state-layer shrink-0 -mr-xs flex items-center justify-center rounded-sm leading-none transition-colors outline-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `size-5 [&>svg]:size-icon-sm` · normal → `size-6 [&>svg]:size-icon-md` · large → `size-7 [&>svg]:size-icon-lg`
- `variant`: solid → `text-current/70 hover:text-current` · outline → `text-current/70 hover:text-current` · soft → `text-color-muted-readable/70 hover:text-color-muted-readable`
