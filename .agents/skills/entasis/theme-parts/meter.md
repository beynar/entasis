# meter theme parts

Generated by `npm run generate:component-contract` from `Meter`'s theme file. Do not edit.

- Registry key: `meter`, the key under `<Theme components>`
- Setter: `setMeterTheme` from `entasis/meter`
- Parts: 14. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex flex-col relative`
- `size`: small → `gap-xs` · normal → `gap-md` · large → `gap-lg`

## `header`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex w-full items-center justify-between relative`
- `size`: small → `gap-xs` · normal → `gap-md` · large → `gap-lg`

## `container`

- Element: `<div>`
- Variants: `first` (true) · `last` (true) — bold is the default
- Base: `block relative`
- `first`: true → `rounded-l-full`
- `last`: true → `rounded-r-full`

## `label`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-sm text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `helper`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-sm text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 text-sm`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `progress`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `appearance-none block w-full h-1 !border-none !bg-color overflow-hidden text-primary rounded-[inherit] `
- `size`: small → `h-1` · normal → `h-2` · large → `h-3`

## `track`

- Element: `<div>`
- Variants: `size` (small | normal | large) · `labelsPosition` (top | bottom | both) — bold is the default
- Base: `flex items-center justify-start bg-neutral-muted/50 rounded-full relative`
- `size`: small → `h-1` · normal → `h-2` · large → `h-3`
- `labelsPosition`: top → `mt-xl` · bottom → `mb-xl` · both → `mt-xl mb-xl`

## `indicator`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) · `position` (**top** | bottom) — bold is the default
- Base: `absolute whitespace-nowrap w-fit rounded-sm text-color-readable mx-auto left-0 right-0 leading-none`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `position`: top → `bottom-full	` · bottom → `top-full`

## `legend`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex flex-col`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `legendItem`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex items-center gap-md [&_*]:text-color-readable`
- `size`: small → `gap-sm` · normal → `gap-md` · large → `gap-md`

## `legendIcon`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `[&>svg]:size-full`
- `size`: small → `w-3 h-3` · normal → `w-4 h-4` · large → `w-5 h-5`

## `legendLabel`

- Element: `<span>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `legendPercentage`

- Element: `<span>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 font-medium`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
