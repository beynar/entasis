# overlay theme parts

Generated by `npm run generate:component-contract` from `Overlay`'s theme file. Do not edit.

- Registry key: `overlay`, the key under `<Theme components>`
- Setter: `setOverlayTheme` from `entasis/overlay`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `position` (**fill** | top | bottom) · `open` (**true** | false) · `showOn` (**always** | hover | focus) — bold is the default
- Base: `pointer-events-none absolute inset-0 z-10 flex overflow-hidden rounded-[inherit] text-white opacity-100 transition-opacity duration-normal ease-standard motion-reduce:transition-none`
- `position`: fill → `items-center` · top → `items-start` · bottom → `items-end`
- `open`: true → _none_ · false → `opacity-0`
- `showOn`: always → _none_ · hover → _none_ · focus → _none_
- Compound: open=true & showOn=hover → `opacity-0` · open=true & showOn=focus → `opacity-0`

## `scrim`

- Element: `<div>`
- Variants: `position` (**fill** | top | bottom) — bold is the default
- Base: `pointer-events-none absolute inset-0`
- `position`: fill → `bg-black/55` · top → `-z-[1] bg-linear-to-b from-black/80 via-black/40 to-transparent` · bottom → `-z-[1] bg-linear-to-t from-black/80 via-black/40 to-transparent`

## `content`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `align` (start | **center** | end) · `position` (**fill** | top | bottom) · `open` (**true** | false) · `showOn` (**always** | hover | focus) — bold is the default
- Base: `relative z-[1] isolate flex w-full min-w-0 flex-col will-change-transform transition-transform duration-normal ease-standard motion-reduce:transition-none`
- `size`: small → `gap-md p-xl` · normal → `gap-lg p-layout-md` · large → `gap-xl p-layout-lg`
- `align`: start → `items-start text-left` · center → `items-center text-center` · end → `items-end text-right`
- `position`: fill → _none_ · top → _none_ · bottom → _none_
- `open`: true → _none_ · false → `pointer-events-none`
- `showOn`: always → `pointer-events-auto` · hover → `pointer-events-none` · focus → `pointer-events-none`
- Compound: position=top & open=false → `-translate-y-2` · position=bottom & open=false → `translate-y-2` · position=top & open=true & showOn=hover → `-translate-y-2` · position=bottom & open=true & showOn=hover → `translate-y-2` · position=top & open=true & showOn=focus → `-translate-y-2` · position=bottom & open=true & showOn=focus → `translate-y-2`

## `header`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex max-w-xl flex-col`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`

## `title`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `font-semibold text-balance`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `max-w-xl text-white/70 text-balance`
- `size`: small → `text-xs/relaxed` · normal → `text-xs/relaxed` · large → `text-sm/relaxed`

## `body`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `max-w-xl text-white`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `actions`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `align` (start | **center** | end) — bold is the default
- Base: `flex w-full max-w-sm flex-wrap items-center`
- `size`: small → `gap-sm` · normal → `gap-md` · large → `gap-md`
- `align`: start → `justify-start` · center → `justify-center` · end → `justify-end`
