# popover theme parts

Generated by `npm run generate:component-contract` from `Popover`'s theme file. Do not edit.

- Registry key: `popover`, the key under `<Theme components>`
- Setter: `setPopoverTheme` from `entasis/popover`
- Parts: 3. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: { x: 0, y: 0, scale: 0.98, opacity: 0 }, out: { x: 0, y: 0, scale: 0.98, opacity: 0 } }`
- `mode`: floating → `{}` · inline → `{}` · mobileSheet → `{ in: { y: '100%', scale: 1, opacity: 1 }, out: { y: '100%', scale: 1, opacity: 1 } }`

## `root`

- Element: `<dialog>`
- Variants: `mode` (**floating** | inline | mobileSheet) — bold is the default
- Base: `fixed m-0 border-none bg-transparent p-0 outline-none`
- `mode`: floating → `top-0 left-0 h-fit w-fit` · inline → `static h-fit w-fit` · mobileSheet → `inset-0 flex h-dvh w-dvw max-w-none items-end justify-center overflow-hidden bg-neutral/40 backdrop-blur-xs`

## `popover`

- Element: `<div>`
- Variants: `size` (small | normal | large) · `mode` (**floating** | inline | mobileSheet) — bold is the default
- Base: `raised-3 bg-surface-floating text-neutral isolate h-fit w-fit rounded-lg text-sm`
- `size`: small → `max-w-3xs w-full p-xs` · normal → `max-w-xs w-full p-md` · large → `max-w-sm w-full p-xl`
- `mode`: floating → _none_ · inline → _none_ · mobileSheet → `max-h-[85dvh] w-full max-w-none overflow-auto rounded-b-none rounded-t-xl pb-[calc(env(safe-area-inset-bottom)+1rem)]`
