# avatar-group theme parts

Generated by `npm run generate:component-contract` from `AvatarGroup`'s theme file. Do not edit.

- Registry key: `avatar-group`, the key under `<Theme components>`
- Setter: `setAvatarGroupTheme` from `entasis/avatar`
- Parts: 2. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `flex gap-[-0.25rem] items-center isolate ml-[0.3rem]`
- `size`: normal → `[&>[data-avatar]]:ml-[-0.45rem]` · small → `[&>[data-avatar]]:ml-[-0.4rem]` · large → `[&>[data-avatar]]:ml-[-0.5rem]`

## `avatarGroupCount`

- Element: `<div>`
- Variants: `size` (**normal** | small | large) — bold is the default
- Base: `bg-surface-floating border-neutral-muted text-neutral text-center rounded-full flex items-center justify-center uppercase font-bold ml-[-0.75rem] z-[+1]`
- `size`: normal → `size-8 text-sm` · small → `size-6 text-xs` · large → `size-10 text-base`
