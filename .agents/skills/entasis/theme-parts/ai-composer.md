# ai-composer theme parts

Generated by `npm run generate:component-contract` from `AIComposer`'s theme file. Do not edit.

- Registry key: `ai-composer`, the key under `<Theme components>`
- Setter: `setAIComposerTheme` from `entasis/ai-composer`
- Parts: 18. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<form data-slot="ai-composer">`
- Variants: `dragState` (idle | potential | valid | invalid) — bold is the default
- Base: `relative grid min-w-0 grid-cols-[minmax(0,1fr)] gap-xs rounded-md border border-neutral-muted bg-surface p-sm transition-colors focus-within:ring-2 focus-within:ring-focus/50`
- `dragState`: idle → _none_ · potential → `border-primary bg-primary/5` · valid → `border-primary bg-primary/5` · invalid → `border-danger bg-danger/5`

## `dropzone`

- Element: `<div data-slot="ai-composer-dropzone">`
- Variants: `state` (idle | potential | valid | invalid) — bold is the default
- Base: `pointer-events-none absolute inset-1 z-20 flex items-center justify-center gap-md rounded-sm border border-dashed bg-surface/95 px-lg text-center text-sm font-medium lift-1`
- `state`: idle → _none_ · potential → `border-primary text-primary-readable` · valid → `border-primary text-primary-readable` · invalid → `border-danger text-danger-readable`

## `dropzoneIcon`

- Element: `<span>`
- Base: `shrink-0`

## `header`

- Element: `<Slot>`
- Base: `min-w-0`

## `files`

- Element: `<div data-slot="ai-composer-files">`
- Base: `flex w-max min-w-full gap-md py-xs`

## `body`

- Element: `<div data-slot="ai-composer-editor">`
- Base: `min-w-0`

## `editor`

- Element: not applied in this component's markup
- Variants: `autoresize` (true | false) — bold is the default
- Base: `py-xs`
- `autoresize`: true → `min-h-row-md` · false → `h-24 min-h-24`

## `toolbar`

- Element: `<RichTextInput>`
- Base: `min-w-0`

## `file`

- Element: `<AIFilePreview>`
- Base: `shrink-0`

## `error`

- Element: `<Alert>`
- Base: `min-w-0`

## `footer`

- Element: `<Slot>` / `<div data-slot="ai-composer-footer">`
- Base: `flex min-w-0 items-center gap-md`

## `actions`

- Element: `<div>`
- Variants: `side` (**start** | end) — bold is the default
- Base: `flex items-center gap-xs`
- `side`: start → `shrink-0` · end → `ml-auto min-w-0 flex-1 justify-end`

## `voiceInput`

- Element: `<div data-slot="ai-composer-voice-input">`
- Variants: `variant` (**compact** | expandable) — bold is the default
- Base: `min-w-0`
- `variant`: compact → `shrink-0` · expandable → `flex min-w-0 flex-1 justify-end`

## `queue`

- Element: `<div data-slot="ai-composer-queue">`
- Base: `grid gap-xs border-b border-neutral-muted pb-md`

## `queueEditing`

- Element: `<div data-slot="ai-composer-queue-editing">`
- Base: `flex min-w-0 items-center justify-between gap-md rounded-sm bg-neutral-muted/50 px-md py-xs text-sm text-neutral/70`

## `queueList`

- Element: `<SortableList>`
- Base: `grid gap-xs`

## `queueItem`

- Element: `<div data-slot="ai-composer-queue-item">`
- Base: `flex min-w-0 flex-1 items-center gap-md`

## `queueText`

- Element: `<span>` / `<div>`
- Base: `min-w-0 flex-1 truncate text-sm text-neutral/70`
