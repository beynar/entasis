# carousel theme parts

Generated by `npm run generate:component-contract` from `Carousel`'s theme file. Do not edit.

- Registry key: `carousel`, the key under `<Theme components>`
- Setter: `setCarouselTheme` from `entasis/carousel`
- Parts: 10. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Base: `relative grid w-full min-w-0 grid-cols-[minmax(0,1fr)] @container/carousel`

## `slider`

- Element: `<div>`
- Base: `display-grid relative whitespace-nowrap overflow-x-auto overflow-y-clip scroll-behavior-smooth overscroll-behavior-x-contain inline-size-full max-inline-size-full box-border scrollbar-width-none`

## `slide`

- Element: `<div>`
- Base: `inline-block whitespace-normal align-top`

## `footer`

- Element: `<div>`
- Base: `flex w-full min-w-0 items-center justify-between gap-md`

## `progress`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `relative min-w-0 flex-1 overflow-hidden rounded-full bg-surface-recessed`
- `size`: small → `h-0.5` · normal → `h-1` · large → `h-1.5`

## `progressFill`

- Element: `<div>`
- Variants: `color` (primary | secondary | **neutral** | danger | success | warning | info) — bold is the default
- Base: `h-full rounded-full transition-[width] duration-fast ease-standard`
- `color`: primary → `bg-primary` · secondary → `bg-secondary` · neutral → `bg-neutral` · danger → `bg-danger` · success → `bg-success` · warning → `bg-warning` · info → `bg-info`

## `navigation`

- Element: `<div>`
- Base: `flex shrink-0 items-center gap-xs ms-auto`

## `navigationButton`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `disabled` (true | false) — bold is the default
- Base: `state-layer rounded-sm cursor-pointer inline-flex items-center justify-center transition-[color,background-color,box-shadow,opacity] duration-fast ease-standard outline-none border-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `h-control-sm min-size-hit-sm` · normal → `h-control-md min-size-hit-md` · large → `h-control-lg min-size-hit-lg`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · neutral → `text-neutral-readable` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable`
- `disabled`: true → `opacity-50 cursor-not-allowed pointer-events-none` · false → _none_

## `dots`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex min-w-0 flex-1 items-center`
- `size`: small → `gap-xs` · normal → `gap-sm` · large → `gap-md`

## `dot`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `active` (true | **false**) — bold is the default
- Base: `rounded-full transition-[background-color,box-shadow] cursor-pointer border-none outline-none`
- `size`: small → `h-1.5 w-1.5` · normal → `h-2 w-2` · large → `h-2.5 w-2.5`
- `color`: primary → `bg-primary` · secondary → `bg-secondary` · neutral → `bg-neutral` · danger → `bg-danger` · success → `bg-success` · warning → `bg-warning` · info → `bg-info`
- `active`: true → `ring-1 ring-color ring-offset-1` · false → _none_
