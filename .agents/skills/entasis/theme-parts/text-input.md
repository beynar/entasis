# text-input theme parts

Generated by `npm run generate:component-contract` from `TextInput`'s theme file. Do not edit.

- Registry key: `text-input`, the key under `<Theme components>`
- Setter: `setTextInputTheme` from `entasis/text-input`
- Parts: 2. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `outline-none flex-1 w-full min-w-0 h-full bg-transparent resize-none placeholder:text-neutral/70 autofill:text-neutral appearance-none text-sm leading-normal`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `flex w-full items-center border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `h-control-sm text-xs` · normal → `h-control-md text-sm` · large → `h-control-lg text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
