# combobox theme parts

Generated by `npm run generate:component-contract` from `Combobox`'s theme file. Do not edit.

- Registry key: `combobox`, the key under `<Theme components>`
- Setter: `setComboboxTheme` from `entasis/combobox`
- Parts: 5. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) · `hasValue` (true | false) · `disabled` (true | **false**) — bold is the default
- Base: `outline-none flex-1 w-full min-w-0 h-full bg-transparent resize-none autofill:text-neutral appearance-none text-sm leading-normal`
- `size`: small → `text-xs placeholder:text-xs` · normal → `text-sm placeholder:text-sm` · large → `text-sm placeholder:text-sm`
- `hasValue`: true → `placeholder:text-neutral` · false → `placeholder:text-neutral/70`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `flex w-full items-center border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `h-control-sm text-xs` · normal → `h-control-md text-sm` · large → `h-control-lg text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `loading`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 p-md text-sm`
- `size`: small → `text-xs p-sm` · normal → `text-sm p-sm` · large → `text-sm p-md`

## `error`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-danger-readable p-md text-sm`
- `size`: small → `text-xs p-sm` · normal → `text-sm p-sm` · large → `text-sm p-md`

## `noOptions`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 p-md text-sm`
- `size`: small → `text-xs p-sm` · normal → `text-sm p-sm` · large → `text-sm p-md`
