# map theme parts

Generated by `npm run generate:component-contract` from `Map`'s theme file. Do not edit.

- Registry key: `map`, the key under `<Theme components>`
- Setter: `setMapTheme` from `entasis/map`
- Parts: 1. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="map">`
- Variants: `size` (small | **normal** | large) · `interactive` (**true** | false) — bold is the default
- Base: `relative w-full overflow-hidden rounded-lg border border-neutral-muted bg-neutral-muted`
- `size`: small → `h-[240px] min-h-[200px]` · normal → `h-[360px] min-h-[280px]` · large → `h-[520px] min-h-[360px]`
- `interactive`: true → _none_ · false → `[&_.maplibregl-canvas]:!cursor-default`
