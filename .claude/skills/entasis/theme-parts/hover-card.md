# hover-card theme parts

Generated by `npm run generate:component-contract` from `HoverCard`'s theme file. Do not edit.

- Registry key: `hover-card`, the key under `<Theme components>`
- Setter: `setHoverCardTheme` from `entasis/hover-card`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: { x: 0, y: 4, scale: 0.98, opacity: 0 }, out: { x: 0, y: 4, scale: 0.98, opacity: 0 }, duration: 'fast', easing: 'enter' }`

## `trigger`

- Element: `<button>` / `<span>`
- Variants: `disabled` (true | **false**) — bold is the default
- Base: `inline-flex w-fit max-w-full outline-none`
- `disabled`: true → `pointer-events-none opacity-55` · false → _none_

## `popover`

- Element: `<Popover>`
- Base: `bg-transparent p-0 shadow-none ring-0`

## `card`

- Element: `<Card>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `w-full raised-3 bg-surface-floating ring-0`
- `size`: small → _none_ · normal → _none_ · large → _none_
