# select theme parts

Generated by `npm run generate:component-contract` from `Select`'s theme file. Do not edit.

- Registry key: `select`, the key under `<Theme components>`
- Setter: `setSelectTheme` from `entasis/select`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `outline-none flex h-full w-full flex-1 cursor-pointer items-center justify-between gap-sm bg-transparent text-left leading-normal select-none before:absolute before:inset-0 before:content-['']`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `state-layer flex w-full cursor-pointer items-center border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `h-control-sm text-xs` · normal → `h-control-md text-sm` · large → `h-control-lg text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `value`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) · `placeholder` (true | **false**) — bold is the default
- Base: `line-clamp-1 flex-1 text-left`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `placeholder`: true → `text-neutral/70` · false → `text-neutral`

## `triggerIcon`

- Element: `<caretDownIcon>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 pointer-events-none shrink-0`
- `size`: small → `size-icon-sm` · normal → `size-icon-md` · large → `size-icon-lg`

## `content`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex flex-col`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `group`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex scroll-my-1 flex-col p-xs`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `groupLabel`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70`
- `size`: small → `px-sm py-xs text-xs` · normal → `px-sm py-xs text-xs` · large → `px-sm py-xs text-xs`

## `separator`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `bg-neutral-muted my-xs h-px`
- `size`: small → _none_ · normal → _none_ · large → _none_
