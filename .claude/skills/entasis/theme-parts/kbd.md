# kbd theme parts

Generated by `npm run generate:component-contract` from `Kbd`'s theme file. Do not edit.

- Registry key: `kbd`, the key under `<Theme components>`
- Setter: `setKbdTheme` from `entasis/kbd`
- Parts: 3. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<kbd>`
- Variants: `size` (small | **normal** | large) · `color` (**neutral** | primary | secondary | danger | success | warning | info) — bold is the default
- Base: `border-neutral-muted shadow-neutral/20 pointer-events-none inline-flex w-fit items-center justify-center rounded-sm border font-sans font-medium shadow-[0_1px_0] select-none`
- `size`: small → `h-4 min-w-4 gap-micro px-xs text-xs [&_svg:not([class*=size-])]:size-2.5` · normal → `h-5 min-w-5 gap-xs px-xs text-sm [&_svg:not([class*=size-])]:size-3` · large → `h-6 min-w-6 gap-xs px-sm text-sm [&_svg:not([class*=size-])]:size-3.5`
- `color`: neutral → `bg-color-muted text-color-muted-readable` · primary → `bg-color-muted text-color-muted-readable` · secondary → `bg-color-muted text-color-muted-readable` · danger → `bg-color-muted text-color-muted-readable` · success → `bg-color-muted text-color-muted-readable` · warning → `bg-color-muted text-color-muted-readable` · info → `bg-color-muted text-color-muted-readable`

## `group`

- Element: `<kbd>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `inline-flex items-center`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`

## `separator`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 select-none`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`
