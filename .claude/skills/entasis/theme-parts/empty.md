# empty theme parts

Generated by `npm run generate:component-contract` from `Empty`'s theme file. Do not edit.

- Registry key: `empty`, the key under `<Theme components>`
- Setter: `setEmptyTheme` from `entasis/empty`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `mode` (**normal** | card) · `bordered` (true | **false**) — bold is the default
- Base: `flex w-full min-w-0 flex-1 flex-col items-center justify-center rounded-lg text-center text-balance`
- `size`: small → `gap-lg p-xl` · normal → `gap-xl p-layout-md` · large → `gap-layout-md p-layout-lg`
- `mode`: normal → _none_ · card → `bg-surface-raised raised-sm`
- `bordered`: true → `border border-dashed border-neutral-muted` · false → _none_

## `header`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex max-w-sm flex-col items-center`
- `size`: small → `gap-sm` · normal → `gap-md` · large → `gap-md`

## `media`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `mediaVariant` (**default** | icon) — bold is the default
- Base: `flex shrink-0 items-center justify-center [&_svg]:pointer-events-none [&_svg]:shrink-0`
- `size`: small → `mb-sm` · normal → `mb-md` · large → `mb-lg`
- `mediaVariant`: default → `bg-transparent` · icon → `bg-neutral-muted text-neutral-muted-readable rounded-md`
- Compound: mediaVariant=icon & size=small → `size-6 [&_svg:not([class*=size-])]:size-3.5` · mediaVariant=icon & size=normal → `size-8 [&_svg:not([class*=size-])]:size-4` · mediaVariant=icon & size=large → `size-10 [&_svg:not([class*=size-])]:size-5`

## `title`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral font-medium tracking-tight`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 [&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary-readable`
- `size`: small → `text-xs/relaxed` · normal → `text-sm/relaxed` · large → `text-base/relaxed`

## `content`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex w-full max-w-sm min-w-0 flex-col items-center text-balance`
- `size`: small → `gap-md text-xs` · normal → `gap-md text-sm` · large → `gap-lg text-base`

## `actions`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex flex-wrap items-center justify-center`
- `size`: small → `gap-sm` · normal → `gap-md` · large → `gap-md`

## `footer`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 flex items-center justify-center [&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary-readable`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`
