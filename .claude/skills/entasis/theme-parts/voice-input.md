# voice-input theme parts

Generated by `npm run generate:component-contract` from `VoiceInput`'s theme file. Do not edit.

- Registry key: `voice-input`, the key under `<Theme components>`
- Setter: `setVoiceInputTheme` from `entasis/voice-input`
- Parts: 11. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: not applied in this component's markup
- Variants: `variant` (**default** | expandable | compact) · `expanded` (**true** | false) — bold is the default
- Base: `min-w-0`
- `variant`: default → `w-full` · expandable → _none_ · compact → `w-fit`
- `expanded`: true → _none_ · false → _none_
- Compound: variant=expandable & expanded=true → `w-full` · variant=expandable & expanded=false → `w-fit`

## `inputContainer`

- Element: not applied in this component's markup
- Variants: `size` (small | **normal** | large) · `recording` (true | **false**) · `playback` (true | **false**) · `error` (true | **false**) · `disabled` (true | **false**) · `variant` (**default** | expandable | compact) · `expanded` (**true** | false) — bold is the default
- Base: `relative ml-auto flex w-full min-w-0 items-center overflow-hidden rounded-md border border-neutral-muted bg-surface-raised px-lg transition-[max-width,background-color,border-color,box-shadow] duration-slow ease-standard motion-reduce:transition-none focus-within:border-color/60 focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `min-h-row-sm gap-sm py-sm` · normal → `min-h-row-md gap-md py-sm` · large → `min-h-row-lg gap-md py-md`
- `recording`: true → `border-color/50` · false → _none_
- `playback`: true → _none_ · false → _none_
- `error`: true → `border-danger focus-within:border-danger focus-within:ring-danger/50` · false → _none_
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
- `variant`: default → `max-w-full` · expandable → _none_ · compact → _none_
- `expanded`: true → _none_ · false → _none_
- Compound: variant=expandable & expanded=true → `max-w-full` · variant=expandable & expanded=false → `gap-0` · variant=compact & expanded=true → `max-w-full` · variant=compact & expanded=false → `gap-0 overflow-visible rounded-full border-0 bg-transparent px-0 py-0` · variant=expandable & expanded=false & size=small → `max-w-[calc(2rem+2px)] border-transparent bg-transparent px-0` · variant=expandable & expanded=false & size=normal → `max-w-[calc(2.25rem+2px)] border-transparent bg-transparent px-0` · variant=expandable & expanded=false & size=large → `max-w-[calc(2.5rem+2px)] border-transparent bg-transparent px-0` · variant=compact & expanded=false & size=small → `min-h-8 max-w-8` · variant=compact & expanded=false & size=normal → `min-h-9 max-w-9` · variant=compact & expanded=false & size=large → `min-h-10 max-w-10` · variant=compact & expanded=false & playback=true & size=small → `max-w-24` · variant=compact & expanded=false & playback=true & size=normal → `max-w-[6.75rem]` · variant=compact & expanded=false & playback=true & size=large → `max-w-[7.5rem]`

## `content`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `variant` (**default** | expandable | compact) · `expanded` (**true** | false) — bold is the default
- Base: `flex min-w-0 flex-1 items-center overflow-hidden transition-[opacity,transform] duration-normal ease-standard motion-reduce:transition-none`
- `size`: small → `gap-sm` · normal → `gap-md` · large → `gap-md`
- `variant`: default → _none_ · expandable → _none_ · compact → _none_
- `expanded`: true → _none_ · false → _none_
- Compound: variant=expandable & expanded=false → `pointer-events-none basis-0 translate-x-2 opacity-0` · variant=expandable & expanded=true → `translate-x-0 opacity-100 delay-75`

## `action`

- Element: `<FieldActionButton>`
- Variants: `size` (small | **normal** | large) · `variant` (**default** | expandable | compact) · `recording` (true | **false**) — bold is the default
- Base: _none_
- `size`: small → _none_ · normal → _none_ · large → _none_
- `variant`: default → _none_ · expandable → _none_ · compact → `relative isolate !m-0 !self-auto !rounded-full !overflow-visible before:pointer-events-none before:absolute before:inset-0 before:-z-10 before:rounded-full before:border before:border-color/55 before:[opacity:calc(var(--voice-input-level)*0.7)] before:[transform:scale(calc(1+var(--voice-input-level)*0.55))] before:transition-[transform,opacity] before:duration-fast before:ease-standard before:content-[""] after:pointer-events-none after:absolute after:inset-0 after:-z-10 after:rounded-full after:border after:border-color/30 after:[opacity:calc(var(--voice-input-level)*0.45)] after:[transform:scale(calc(1+var(--voice-input-level)*1.05))] after:transition-[transform,opacity] after:duration-normal after:ease-standard after:content-[""] motion-reduce:before:transition-none motion-reduce:after:transition-none`
- `recording`: true → _none_ · false → _none_
- Compound: variant=compact & recording=true → `!border-color !bg-color !text-color-contrast lift-1` · variant=compact & size=small → `!size-8` · variant=compact & size=normal → `!size-9` · variant=compact & size=large → `!size-10`

## `playbackAction`

- Element: `<FieldActionButton>`
- Variants: `size` (small | **normal** | large) · `variant` (**default** | expandable | compact) · `playing` (true | **false**) — bold is the default
- Base: _none_
- `size`: small → _none_ · normal → _none_ · large → _none_
- `variant`: default → _none_ · expandable → _none_ · compact → `!m-0 !self-auto !rounded-full`
- `playing`: true → `bg-color-muted text-color-muted-readable` · false → _none_
- Compound: variant=compact & size=small → `!size-8` · variant=compact & size=normal → `!size-9` · variant=compact & size=large → `!size-10`

## `clearAction`

- Element: `<FieldActionButton>`
- Variants: `size` (small | **normal** | large) · `variant` (**default** | expandable | compact) — bold is the default
- Base: _none_
- `size`: small → _none_ · normal → _none_ · large → _none_
- `variant`: default → _none_ · expandable → _none_ · compact → `!m-0 !self-auto !rounded-full`
- Compound: variant=compact & size=small → `!size-8` · variant=compact & size=normal → `!size-9` · variant=compact & size=large → `!size-10`

## `waveformContainer`

- Element: `<div data-slot="voice-input-waveform">`
- Variants: `size` (small | **normal** | large) · `interactive` (true | **false**) · `hidden` (true | **false**) — bold is the default
- Base: `relative min-w-0 flex-1 rounded-sm outline-none`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `interactive`: true → `cursor-pointer has-[input:focus-visible]:ring-2 has-[input:focus-visible]:ring-focus/50` · false → _none_
- `hidden`: true → `hidden` · false → _none_

## `waveform`

- Element: `<canvas>`
- Variants: `size` (small | **normal** | large) · `recording` (true | **false**) · `hidden` (true | **false**) — bold is the default
- Base: `block w-full min-w-0 text-neutral/70 transition-colors`
- `size`: small → `h-6` · normal → `h-8` · large → `h-9`
- `recording`: true → `text-color-readable` · false → _none_
- `hidden`: true → `hidden` · false → _none_

## `waveformInput`

- Element: `<input data-slot="voice-input-waveform-input">`
- Base: `absolute inset-0 z-10 m-0 h-full w-full cursor-pointer touch-none appearance-none bg-transparent opacity-0 focus:outline-none disabled:cursor-not-allowed`

## `timer`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) · `playback` (true | **false**) — bold is the default
- Base: `shrink-0 text-right font-medium tabular-nums text-neutral/70`
- `size`: small → `min-w-8 text-xs` · normal → `min-w-9 text-sm` · large → `min-w-10 text-base`
- `playback`: true → `min-w-fit` · false → _none_

## `error`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `min-w-0 flex-1 truncate text-danger-readable`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
