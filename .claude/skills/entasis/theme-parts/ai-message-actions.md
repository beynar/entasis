# ai-message-actions theme parts

Generated by `npm run generate:component-contract` from `AIMessageActions`'s theme file. Do not edit.

- Registry key: `ai-message-actions`, the key under `<Theme components>`
- Setter: `setAIMessageActionsTheme` from `entasis/ai-message-actions`
- Parts: 3. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="ai-message-actions">`
- Variants: `role` (user | **assistant** | system | tool) · `visibility` (hover | **always** | none) · `size` (small | **normal** | large) — bold is the default
- Base: `flex min-h-[var(--control-height-sm)] w-full flex-wrap items-center gap-xs px-xs transition-opacity motion-reduce:transition-none`
- `role`: user → `justify-end` · assistant → `justify-start` · system → `justify-center` · tool → `justify-start`
- `visibility`: hover → `opacity-0 group-hover/message:opacity-100 group-focus-within/message:opacity-100 hover:opacity-100 focus-within:opacity-100` · always → `opacity-100` · none → `hidden`
- `size`: small → `min-h-6 gap-micro px-micro` · normal → `min-h-[var(--control-height-sm)] gap-xs px-xs` · large → `min-h-[var(--control-height-md)] gap-sm px-xs`

## `button`

- Element: `<Button>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 hover:text-neutral`
- `size`: small → `!h-6 [&_svg]:!size-3` · normal → _none_ · large → `[&_svg]:!size-[1.125rem]`

## `error`

- Element: `<div data-slot="ai-message-actions-error">`
- Base: `w-full basis-full pt-xs`
