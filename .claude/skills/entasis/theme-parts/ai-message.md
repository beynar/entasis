# ai-message theme parts

Generated by `npm run generate:component-contract` from `AIMessage`'s theme file. Do not edit.

- Registry key: `ai-message`, the key under `<Theme components>`
- Setter: `setAIMessageTheme` from `entasis/ai-message`
- Parts: 7. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<article data-slot="ai-message">`
- Variants: `role` (user | **assistant** | system | tool) · `size` (small | **normal** | large) — bold is the default
- Base: `group/message flex w-full min-w-0 gap-lg`
- `role`: user → `justify-end` · assistant → `justify-start` · system → `justify-center` · tool → `justify-start`
- `size`: small → `py-sm` · normal → `py-md` · large → `py-md`

## `body`

- Element: `<div data-slot="ai-message-body">`
- Variants: `role` (user | **assistant** | system | tool) · `size` (small | **normal** | large) · `variant` (**bubble** | minimal) — bold is the default
- Base: `relative flex min-w-0 max-w-[min(38rem,82%)] flex-col`
- `role`: user → `items-end` · assistant → `items-start` · system → `max-w-full items-center` · tool → `w-full max-w-full items-start`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`
- `variant`: bubble → _none_ · minimal → _none_
- Compound: role=assistant & variant=minimal → `max-w-[min(44rem,100%)]`

## `header`

- Element: `<div data-slot="ai-message-name">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `px-xs font-medium text-neutral/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `bubble`

- Element: `<div data-slot="ai-message-content">`
- Variants: `role` (user | **assistant** | system | tool) · `size` (small | **normal** | large) · `variant` (**bubble** | minimal) — bold is the default
- Base: `min-w-0 max-w-full break-words [&_*:first-child]:mt-0 [&_*:last-child]:mb-0`
- `role`: user → `rounded-lg rounded-br-sm` · assistant → `rounded-lg rounded-bl-sm bg-neutral-muted text-neutral` · system → `rounded-sm border border-neutral-muted bg-surface text-neutral/70 [&_*]:!text-neutral/70` · tool → `w-full max-w-none rounded-lg rounded-bl-sm bg-neutral-muted text-neutral`
- `size`: small → `px-lg py-md text-xs leading-normal` · normal → `px-xl py-md text-sm leading-relaxed` · large → `px-layout-sm py-lg text-base leading-relaxed`
- `variant`: bubble → _none_ · minimal → _none_
- Compound: role=user & variant=bubble → `bg-primary text-primary-contrast [&_*]:!text-primary-contrast [&_code]:!bg-primary-contrast/15` · role=user & variant=minimal → `bg-neutral-muted text-neutral [&_*]:!text-neutral [&_code]:!bg-neutral/10` · role=system & size=small → `px-md py-xs !text-xs` · role=system & size=normal → `px-lg py-sm !text-xs` · role=system & size=large → `px-xl py-md !text-sm` · role=assistant | tool & variant=minimal → `rounded-none bg-transparent p-0`

## `markdown`

- Element: `<Markdown>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `min-w-0`
- `size`: small → `!text-xs !leading-normal` · normal → `!text-sm` · large → `!text-base`

## `files`

- Element: `<AIMessageFiles>`
- Base: `max-w-full min-w-0`

## `actions`

- Element: `<AIMessageActions>`
- Variants: `visibility` (**hover** | always | none) — bold is the default
- Base: `absolute left-0 top-full z-10 w-full transition-opacity motion-reduce:transition-none`
- `visibility`: hover → `pointer-events-none opacity-0 group-hover/message:pointer-events-auto group-hover/message:opacity-100 group-focus-within/message:pointer-events-auto group-focus-within/message:opacity-100` · always → `opacity-100` · none → `hidden`
