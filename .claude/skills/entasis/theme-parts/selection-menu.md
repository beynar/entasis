# selection-menu theme parts

Generated by `npm run generate:component-contract` from `SelectionMenu`'s theme file. Do not edit.

- Registry key: `selection-menu`, the key under `<Theme components>`
- Setter: `setSelectionMenuTheme` from `entasis/selection-menu`
- Parts: 2. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `popover`

- Element: `<Popover>`
- Base: `bg-surface-floating max-w-[calc(100vw-1rem)] p-0 shadow-none ring-0`

## `content`

- Element: `<div data-slot="selection-menu-content">`
- Base: `max-w-[calc(100vw-1rem)]`
