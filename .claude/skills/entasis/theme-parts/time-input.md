# time-input theme parts

Generated by `npm run generate:component-contract` from `TimeInput`'s theme file. Do not edit.

- Registry key: `time-input`, the key under `<Theme components>`
- Setter: `setTimeInputTheme` from `entasis/time-input`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `outline-none flex-1 w-full min-w-0 h-full bg-transparent resize-none placeholder:text-neutral/70 autofill:text-neutral appearance-none text-sm leading-normal`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `flex w-full items-center border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `h-control-sm text-xs` · normal → `h-control-md text-sm` · large → `h-control-lg text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `popover`

- Element: `<Popover>`
- Base: `w-48 max-w-[calc(100vw-2rem)]`

## `picker`

- Element: `<div>`
- Base: `grid w-full grid-cols-2 gap-md`

## `pickerColumn`

- Element: `<div>`
- Base: `min-w-0`

## `pickerLabel`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 px-sm pb-xs font-medium`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `pickerScrollArea`

- Element: `<ScrollArea>`
- Base: `flex max-h-44 flex-col`

## `pickerOption`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `selected` (true | **false**) — bold is the default
- Base: `state-layer w-full rounded-md font-mono text-center tabular-nums outline-none transition-colors focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `px-sm py-xs text-xs` · normal → `px-md py-xs text-sm` · large → `px-md py-sm text-sm`
- `selected`: true → _none_ · false → `text-neutral`
