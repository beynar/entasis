# mini-calendar theme parts

Generated by `npm run generate:component-contract` from `MiniCalendar`'s theme file. Do not edit.

- Registry key: `mini-calendar`, the key under `<Theme components>`
- Setter: `setMiniCalendarTheme` from `entasis/mini-calendar`
- Parts: 7. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `inline-flex w-fit items-center rounded-lg border bg-surface`
- `size`: small → `gap-micro p-micro` · normal → `gap-xs p-xs` · large → `gap-sm p-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `navButton`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `state-layer inline-flex shrink-0 items-center justify-center rounded-md text-neutral/70 outline-none transition-colors hover:text-neutral focus-visible:ring-2 focus-visible:ring-focus/50 disabled:pointer-events-none`
- `size`: small → `size-7` · normal → `size-9` · large → `size-11`

## `days`

- Element: `<div>`
- Base: `grid -m-0.5 overflow-hidden p-micro`

## `track`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `col-start-1 row-start-1 flex items-center`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`

## `day`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | danger | success | warning | info | **neutral**) · `selected` (true | **false**) · `today` (true | **false**) · `disabled` (true | **false**) — bold is the default
- Base: `state-layer relative inline-flex shrink-0 flex-col items-center justify-center rounded-md leading-none outline-none transition-colors focus-visible:ring-2 focus-visible:ring-focus/50 disabled:pointer-events-none`
- `size`: small → `min-w-11 gap-micro px-md py-xs` · normal → `min-w-14 gap-micro px-lg py-sm` · large → `min-w-16 gap-xs px-xl py-md`
- `color`: primary → _none_ · secondary → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_ · neutral → _none_
- `selected`: true → `raised` · false → `text-neutral`
- `today`: true → _none_ · false → _none_
- `disabled`: true → _none_ · false → `cursor-pointer`
- Compound: selected=true & color=primary → `bg-primary text-primary-contrast` · selected=true & color=secondary → `bg-secondary text-secondary-contrast` · selected=true & color=danger → `bg-danger text-danger-contrast` · selected=true & color=success → `bg-success text-success-contrast` · selected=true & color=warning → `bg-warning text-warning-contrast` · selected=true & color=info → `bg-info text-info-contrast` · selected=true & color=neutral → `bg-neutral text-neutral-contrast` · selected=false & today=true & color=primary → `bg-primary/10 text-primary-muted-readable` · selected=false & today=true & color=secondary → `bg-secondary/10 text-secondary-muted-readable` · selected=false & today=true & color=danger → `bg-danger/10 text-danger-muted-readable` · selected=false & today=true & color=success → `bg-success/10 text-success-muted-readable` · selected=false & today=true & color=warning → `bg-warning/10 text-warning-muted-readable` · selected=false & today=true & color=info → `bg-info/10 text-info-muted-readable` · selected=false & today=true & color=neutral → `bg-neutral/10 text-neutral-muted-readable`

## `dayMonth`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `font-medium uppercase tracking-wide opacity-70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `dayNumber`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `font-semibold tabular-nums`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
