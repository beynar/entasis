# marquee theme parts

Generated by `npm run generate:component-contract` from `Marquee`'s theme file. Do not edit.

- Registry key: `marquee`, the key under `<Theme components>`
- Setter: `setMarqueeTheme` from `entasis/marquee`
- Parts: 2. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="marquee">`
- Variants: `direction` (**left** | up) · `size` (small | **normal** | large) · `fade` (**true** | false) — bold is the default
- Base: `group flex overflow-hidden relative`
- `direction`: left → `flex-row` · up → `flex-col`
- `size`: small → `gap-md` · normal → `gap-xl` · large → `gap-layout-md`
- `fade`: true → `scroll-fade-static` · false → _none_
- Compound: fade=true & direction=left → `scroll-fade-x` · fade=true & direction=up → `scroll-fade-y`

## `inner`

- Element: `<div data-slot="marquee-copy">`
- Variants: `direction` (**left** | up) · `size` (small | **normal** | large) · `pauseOnHover` (true | **false**) · `reverse` (true | **false**) — bold is the default
- Base: `flex shrink-0 justify-around [--gap:1rem] whitespace-nowrap`
- `direction`: left → `flex-row animate-marquee-left` · up → `flex-col animate-marquee-up`
- `size`: small → `gap-md` · normal → `gap-xl` · large → `gap-layout-md`
- `pauseOnHover`: true → `group-hover:[--marquee-animation-play-state:paused]` · false → _none_
- `reverse`: true → `[--marquee-animation-direction:reverse]` · false → _none_
