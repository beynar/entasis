# spinner theme parts

Generated by `npm run generate:component-contract` from `Spinner`'s theme file. Do not edit.

- Registry key: `spinner`, the key under `<Theme components>`
- Setter: `setSpinnerTheme` from `entasis/spinner`
- Parts: 3. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<span data-slot="spinner">`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | danger | success | warning | info | **neutral**) — bold is the default
- Base: `inline-flex w-fit shrink-0 items-center justify-center align-middle`
- `size`: small → `gap-xs` · normal → `gap-sm` · large → `gap-md`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable` · neutral → `text-neutral`

## `indicator`

- Element: `<SpinnerIndicator>`
- Variants: `size` (small | **normal** | large) · `variant` (**default** | grid | pulse | puff | lines | circles) — bold is the default
- Base: `relative box-border shrink-0`
- `size`: small → `[--spinner-size:1rem]` · normal → `[--spinner-size:1.25rem]` · large → `[--spinner-size:1.5rem]`
- `variant`: default → `ui-spinner` · grid → `grid grid-cols-3 place-items-center` · pulse → `flex items-center justify-center gap-[calc(var(--spinner-size)*0.1)]` · puff → `block` · lines → `block` · circles → `block`

## `label`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
