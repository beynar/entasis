# stepper theme parts

Generated by `npm run generate:component-contract` from `Stepper`'s theme file. Do not edit.

- Registry key: `stepper`, the key under `<Theme components>`
- Setter: `setStepperTheme` from `entasis/stepper`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: {}, out: {}, duration: 'slow', easing: 'standard' }`

## `root`

- Element: `<div>`
- Variants: `mode` (classic | vertical) — bold is the default
- Base: `relative w-full max-w-full min-w-0 transition-[height] data-[animating=true]:overflow-hidden`
- `mode`: classic → _none_ · vertical → _none_

## `container`

- Element: `<div>`
- Variants: `mode` (classic | vertical) — bold is the default
- Base: `pointer-events-none absolute top-0 left-0 grid min-w-0`
- `mode`: classic → _none_ · vertical → _none_

## `step`

- Element: `<div>`
- Variants: `mode` (classic | vertical) — bold is the default
- Base: `pointer-events-none h-fit w-full min-w-0 flex-1 opacity-0 transition-opacity focus:outline-none data-[step-active=true]:pointer-events-auto data-[step-active=true]:opacity-100 starting:data-[step-active=true]:opacity-0`
- `mode`: classic → _none_ · vertical → _none_
