# globe theme parts

Generated by `npm run generate:component-contract` from `Globe`'s theme file. Do not edit.

- Registry key: `globe`, the key under `<Theme components>`
- Setter: `setGlobeTheme` from `entasis/globe`
- Parts: 1. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<canvas>`
- Variants: `draggable` (**true** | false) · `ready` (true | **false**) — bold is the default
- Base: `origin-center mx-auto block aspect-square size-[400px] max-w-full touch-none select-none transition-opacity duration-slower ease-enter`
- `draggable`: true → `cursor-grab` · false → _none_
- `ready`: true → `opacity-100` · false → `opacity-0`
