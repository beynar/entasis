# rich-text-input theme parts

Generated by `npm run generate:component-contract` from `RichTextInput`'s theme file. Do not edit.

- Registry key: `rich-text-input`, the key under `<Theme components>`
- Setter: `setRichTextInputTheme` from `entasis/rich-text-input`
- Parts: 19. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="rich-text-input-root">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `text-neutral min-w-0 w-full`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `disabled`: true → `opacity-60` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `block w-full min-w-0`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `disabled`: true → _none_ · false → _none_

## `fixedToolbar`

- Element: `<div data-slot="rich-text-input-toolbar">`
- Variants: `size` (small | **normal** | large) · `standalone` (true | **false**) — bold is the default
- Base: `border-neutral-muted bg-surface-raised/70 mb-md flex min-w-0 items-center overflow-x-auto rounded-md border p-xs`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-xs`
- `standalone`: true → `mb-xs rounded-none border-0 bg-transparent p-0` · false → _none_

## `viewport`

- Element: `<div data-slot="rich-text-input">`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `min-w-0`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `scrollArea`

- Element: `<ScrollArea>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) · `standalone` (true | **false**) · `height` (**default** | custom | uncapped) — bold is the default
- Base: `group/rich-text-input flex min-w-0 flex-col overflow-hidden rounded-md border border-neutral-muted bg-surface-raised text-neutral transition-[color,background-color,border-color,box-shadow] focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `max-h-40 min-h-20` · normal → `max-h-48 min-h-24` · large → `max-h-64 min-h-32`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
- `standalone`: true → `rounded-none border-0 bg-transparent focus-within:ring-0` · false → _none_
- `height`: default → _none_ · custom → `!max-h-[var(--rich-text-input-max-height)]` · uncapped → `!max-h-none`

## `editorShell`

- Element: `<div data-slot="rich-text-input-shell">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `relative min-w-0`
- `size`: small → `p-md` · normal → `p-lg` · large → `p-xl`

## `editor`

- Element: `<div data-slot="ai-composer-editor">`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: _none_
- `size`: small → `min-h-16 text-xs leading-normal` · normal → `min-h-18 text-sm leading-normal` · large → `min-h-24 text-base leading-normal`
- `disabled`: true → `cursor-not-allowed` · false → _none_

## `placeholder`

- Element: `<div data-slot="rich-text-input-placeholder">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 pointer-events-none absolute`
- `size`: small → `left-2 top-2 text-xs leading-normal` · normal → `left-3 top-3 text-sm leading-normal` · large → `left-4 top-4 text-base leading-normal`

## `scrollComfortSpacer`

- Element: `<div data-slot="rich-text-input-scroll-comfort">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `pointer-events-none w-full shrink-0`
- `size`: small → `h-row-sm` · normal → `h-row-md` · large → `h-row-lg`

## `floatingPanel`

- Element: `<RichTextInputFormattingToolbar>` / `<Popover>`
- Variants: `size` (small | **normal** | large) · `width` (toolbar | suggestions) — bold is the default
- Base: `bg-surface-floating raised-3 isolate overflow-hidden rounded-lg text-neutral`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `width`: toolbar → `w-fit` · suggestions → `w-72`

## `formattingToolbar`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex min-w-0 max-w-full items-center gap-micro`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `formattingToolbarRail`

- Element: not applied in this component's markup
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex min-w-0 items-center gap-xs overflow-hidden whitespace-nowrap`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `toolbarButton`

- Element: `<Button>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `shrink-0`
- `size`: small → `!size-6 !min-w-6` · normal → `!size-7 !min-w-7` · large → `!size-8 !min-w-8`

## `toolbarIcon`

- Element: not applied in this component's markup
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `shrink-0`
- `size`: small → `size-3.5` · normal → `size-4` · large → `size-4.5`

## `toolbarSeparator`

- Element: not applied in this component's markup
- Variants: `size` (small | normal | large) — bold is the default
- Base: `bg-neutral-muted mx-xs h-5 w-px shrink-0`
- `size`: small → `h-4` · normal → `h-5` · large → `h-5`

## `linkForm`

- Element: `<form>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex min-w-0 items-center gap-xs`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `linkInput`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `border-neutral-muted bg-surface-raised text-neutral placeholder:text-neutral/70 min-w-0 flex-1 rounded-md border outline-none focus:ring-2 focus:ring-focus/50`
- `size`: small → `h-control-sm w-44 px-md text-xs` · normal → `h-control-md w-56 px-md text-sm` · large → `h-control-lg w-64 px-lg text-sm`

## `suggestionsStatus`

- Element: `<div data-slot="ai-composer-suggestions-status">`
- Variants: `size` (small | **normal** | large) · `status` (**loading** | error) — bold is the default
- Base: `border-neutral-muted text-neutral/70 border-t px-md py-sm`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`
- `status`: loading → _none_ · error → `text-danger-readable`

## `suggestionsError`

- Element: `<span data-slot="ai-composer-suggestions-error">`
- Base: `text-danger-readable`
