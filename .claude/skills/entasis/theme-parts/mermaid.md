# mermaid theme parts

Generated by `npm run generate:component-contract` from `Mermaid`'s theme file. Do not edit.

- Registry key: `mermaid`, the key under `<Theme components>`
- Setter: `setMermaidTheme` from `entasis/mermaid`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `group/mermaid relative w-full overflow-hidden rounded-sm border border-neutral-muted bg-surface-raised`
- `size`: small → `h-64 min-h-48` · normal → `h-80 min-h-56` · large → `h-[28rem] min-h-72`

## `container`

- Element: `<div>`
- Base: `absolute inset-0 flex items-start justify-start`

## `svg`

- Element: `<svg>`
- Base: `block max-w-none origin-top-left [&_svg]:block`

## `buttons`

- Element: `<div>`
- Base: `absolute right-2 top-2 z-10 flex items-center gap-xs rounded-sm border border-neutral-muted bg-surface-floating/80 p-xs opacity-0 backdrop-blur transition-opacity group-hover/mermaid:opacity-100 focus-within:opacity-100`

## `error`

- Element: `<div>`
- Base: `absolute inset-0 z-20 flex items-center justify-center overflow-auto p-layout-md text-center text-sm rounded-sm border border-danger/40 bg-danger-muted text-danger-muted-readable`

## `skeleton`

- Element: `<Skeleton>`
- Base: `absolute inset-0 z-10 h-full w-full rounded-none`
