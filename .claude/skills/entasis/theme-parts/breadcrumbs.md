# breadcrumbs theme parts

Generated by `npm run generate:component-contract` from `Breadcrumbs`'s theme file. Do not edit.

- Registry key: `breadcrumbs`, the key under `<Theme components>`
- Setter: `setBreadcrumbsTheme` from `entasis/breadcrumbs`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<nav>`
- Base: `text-neutral/70 flex flex-wrap items-center gap-sm text-sm break-words`

## `item`

- Element: `<li>`
- Variants: `disabled` (true | false) · `active` (true | false) — bold is the default
- Base: `inline-flex items-center gap-sm`
- `disabled`: true → `text-neutral/70 cursor-not-allowed *:pointer-events-none` · false → `cursor-pointer`
- `active`: true → `text-neutral font-normal cursor-auto` · false → `hover:text-neutral`

## `link`

- Element: `<button>` / `<svelte:element>`
- Variants: `disabled` (true | **false**) · `active` (true | false) — bold is the default
- Base: `px-md py-micro  outline-none focus-visible:ring-1 focus-visible:ring-focus/50 focus-visible:ring-offset-1 ring-offset-surface rounded-sm`
- `disabled`: true → _none_ · false → _none_
- `active`: true → `text-neutral font-normal` · false → _none_

## `separator`

- Element: `<Slot>`
- Base: `flex items-center text-neutral/70 [&>svg]:size-icon-sm`

## `ellipsis`

- Element: `<Slot>`
- Base: `flex items-end justify-center`

## `icon`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex-shrink-0`
- `size`: small → `w-3 h-3` · normal → `w-4 h-4` · large → `w-5 h-5`
