# accordion theme parts

Generated by `npm run generate:component-contract` from `Accordion`'s theme file. Do not edit.

- Registry key: `accordion`, the key under `<Theme components>`
- Setter: `setAccordionTheme` from `entasis/accordion`
- Parts: 10. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: { axis: 'y', x: 0, y: 0, scale: 1, opacity: 0.2 }, out: { axis: 'y', x: 0, y: 0, scale: 1, opacity: 0.2 } }`
- `axis`: y → `{}` · x → `{ in: { axis: 'x' }, out: { axis: 'x' } }`

## `root`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) · `variant` (**classic** | card | outline) · `splitted` (true | **false**) — bold is the default
- Base: `w-full h-fit`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `density`: compact → _none_ · normal → _none_ · comfortable → _none_
- `variant`: classic → _none_ · card → _none_ · outline → _none_
- `splitted`: true → `flex flex-col` · false → _none_
- Compound: variant=card & splitted=false → `raised rounded-lg bg-surface-raised` · variant=outline & splitted=false → `rounded-lg border border-neutral-muted` · splitted=true & density=compact → `gap-md` · splitted=true & density=normal → `gap-lg` · splitted=true & density=comfortable → `gap-xl`

## `item`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) · `variant` (**classic** | card | outline) · `splitted` (true | **false**) · `expanded` (true | **false**) — bold is the default
- Base: `w-full isolate relative`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `density`: compact → _none_ · normal → _none_ · comfortable → _none_
- `variant`: classic → _none_ · card → _none_ · outline → _none_
- `splitted`: true → _none_ · false → _none_
- `expanded`: true → _none_ · false → _none_
- Compound: splitted=false → `border-b border-neutral-muted last:border-b-0` · variant=classic & splitted=true → `border-b border-neutral-muted` · variant=card & splitted=true → `raised rounded-lg bg-surface-raised` · variant=outline & splitted=true → `rounded-lg border border-neutral-muted`

## `header`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) — bold is the default
- Base: `flex-1 flex flex-col items-start`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `density`: compact → `gap-0` · normal → `gap-micro` · comfortable → `gap-xs`

## `trigger`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) · `variant` (**classic** | card | outline) — bold is the default
- Base: `group/accordion-trigger cursor-pointer w-full flex items-start justify-between gap-xl text-left transition-[color,background-color,box-shadow,opacity] outline-none rounded-sm focus-visible:ring-2 focus-visible:ring-focus/50 disabled:pointer-events-none disabled:opacity-50`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `density`: compact → `py-md` · normal → `py-md` · comfortable → `py-lg`
- `variant`: classic → _none_ · card → `px-xl` · outline → `px-xl`

## `title`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral font-medium group-hover/accordion-trigger:underline`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`

## `icon`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 block`
- `size`: small → `size-3.5` · normal → `size-4` · large → `size-5`

## `iconWrapper`

- Element: `<span data-slot="accordion-icon-wrapper">`
- Variants: `expanded` (true | **false**) — bold is the default
- Base: `shrink-0 translate-y-0.5 transition-transform`
- `expanded`: true → `rotate-180` · false → _none_

## `content`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) · `variant` (**classic** | card | outline) — bold is the default
- Base: `pt-0 origin-top w-full`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `density`: compact → `pb-md` · normal → `pb-md` · comfortable → `pb-lg`
- `variant`: classic → _none_ · card → `px-xl` · outline → `px-xl`
