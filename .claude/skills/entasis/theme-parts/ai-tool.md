# ai-tool theme parts

Generated by `npm run generate:component-contract` from `AITool`'s theme file. Do not edit.

- Registry key: `ai-tool`, the key under `<Theme components>`
- Setter: `setAIToolTheme` from `entasis/ai-tool`
- Parts: 34. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="ai-tool">`
- Variants: `scope` (**single** | group) · `variant` (card | **ghost** | outline | soft) · `tone` (active | success | error | cancelled | **default**) — bold is the default
- Base: `inline-grid max-w-full min-w-0 align-top text-neutral transition-[color,background-color,box-shadow]`
- `scope`: single → _none_ · group → _none_
- `variant`: card → _none_ · ghost → _none_ · outline → _none_ · soft → _none_
- `tone`: active → _none_ · success → _none_ · error → _none_ · cancelled → _none_ · default → _none_
- Compound: scope=single & variant=card → `rounded-lg bg-surface-raised ring-1 ring-neutral-muted lift-1` · scope=single & variant=outline → `rounded-lg ring-1 ring-neutral-muted` · scope=single & variant=soft & tone=active → `rounded-lg bg-info/5` · scope=single & variant=soft & tone=success → `rounded-lg bg-success/5` · scope=single & variant=soft & tone=error → `rounded-lg bg-danger/5` · scope=single & variant=soft & tone=cancelled → `rounded-lg bg-neutral/5` · scope=single & variant=soft & tone=default → `rounded-lg bg-neutral/5`

## `accordionRoot`

- Element: not applied in this component's markup
- Variants: `scope` (single | group | calls) — bold is the default
- Base: `max-w-full gap-xs`
- `scope`: single → `w-[min(32rem,100%)]` · group → `w-fit` · calls → `w-[min(32rem,calc(100vw-2rem))]`

## `accordionItem`

- Element: not applied in this component's markup
- Variants: `scope` (single | group | calls) · `variant` (card | **ghost** | outline | soft) — bold is the default
- Base: `max-w-full`
- `scope`: single → `w-full` · group → `w-fit` · calls → `w-full`
- `variant`: card → _none_ · ghost → _none_ · outline → _none_ · soft → _none_
- Compound: scope=calls & variant=card → `mb-md rounded-sm border-b-0 bg-surface-raised ring-1 ring-neutral-muted lift-1 last:mb-0` · scope=calls & variant=outline → `mb-md rounded-sm border-b-0 ring-1 ring-neutral-muted last:mb-0` · scope=calls & variant=soft → `mb-md rounded-sm border-b-0 bg-neutral/5 last:mb-0 has-[[data-tone=active]]:bg-info/5 has-[[data-tone=success]]:bg-success/5 has-[[data-tone=error]]:bg-danger/5`

## `accordionHeader`

- Element: not applied in this component's markup
- Base: `min-w-0 flex-1`

## `accordionTrigger`

- Element: not applied in this component's markup
- Variants: `scope` (single | group | calls) · `variant` (card | **ghost** | outline | soft) · `tone` (active | success | error | cancelled | **default**) · `toggleIcon` (**none** | chevron | 'plus-minus') — bold is the default
- Base: `state-layer relative min-h-row-sm max-w-full items-center gap-md rounded-sm text-left text-xs text-neutral/70 hover:text-neutral`
- `scope`: single → `px-md py-sm` · group → `w-fit` · calls → _none_
- `variant`: card → `px-lg py-md` · ghost → `px-md py-sm` · outline → `px-lg py-md` · soft → `px-lg py-md`
- `tone`: active → _none_ · success → _none_ · error → _none_ · cancelled → _none_ · default → _none_
- `toggleIcon`: none → _none_ · chevron → `pr-layout-md` · 'plus-minus' → `pr-layout-md`
- Compound: scope=group & variant=ghost → `pl-md` · scope=group & variant=card → `bg-surface-raised ring-1 ring-neutral-muted lift-1` · scope=group & variant=outline → `ring-1 ring-neutral-muted` · scope=group & variant=soft & tone=active → `bg-info/5` · scope=group & variant=soft & tone=success → `bg-success/5` · scope=group & variant=soft & tone=error → `bg-danger/5` · scope=group & variant=soft & tone=cancelled → `bg-neutral/5` · scope=group & variant=soft & tone=default → `bg-neutral/5`

## `accordionTitle`

- Element: not applied in this component's markup
- Base: `min-w-0 w-full no-underline group-hover/accordion-trigger:no-underline`

## `accordionIcon`

- Element: not applied in this component's markup
- Base: `size-2.5 text-neutral/45`

## `accordionIconWrapper`

- Element: not applied in this component's markup
- Base: `pointer-events-none absolute top-1/2 right-2 flex size-2.5 -translate-y-1/2 items-center justify-center`

## `accordionContent`

- Element: not applied in this component's markup
- Variants: `scope` (single | group | calls) · `variant` (card | **ghost** | outline | soft) — bold is the default
- Base: `min-w-0 pt-0`
- `scope`: single → `px-md pb-md` · group → `px-0 pb-xs` · calls → `px-0 pb-md`
- `variant`: card → _none_ · ghost → _none_ · outline → _none_ · soft → _none_
- Compound: scope=calls & variant=card → `px-lg pt-md pb-lg` · scope=calls & variant=outline → `px-lg pt-md pb-lg` · scope=calls & variant=soft → `px-lg pt-md pb-lg`

## `title`

- Element: `<div data-slot="ai-tool-group-trigger-content">` / `<div data-slot="ai-tool-call-trigger-content">`
- Base: `flex min-w-0 flex-1 items-center gap-md text-xs`

## `indicator`

- Element: `<span data-slot="ai-tool-call-status-indicator">`
- Variants: `tone` (active | success | error | cancelled | default) — bold is the default
- Base: `flex size-icon-md shrink-0 items-center justify-center`
- `tone`: active → `text-info` · success → `text-success` · error → `text-danger` · cancelled → `text-neutral/45` · default → `text-neutral/45`

## `indicatorDot`

- Element: `<span>`
- Base: `size-1.5 rounded-full bg-current`

## `groupIcon`

- Element: `<span data-slot="ai-tool-group-icon">`
- Variants: `tone` (active | success | error | cancelled | **default**) — bold is the default
- Base: `flex size-icon-md shrink-0 items-center justify-center`
- `tone`: active → `text-info` · success → `text-success` · error → `text-danger` · cancelled → `text-neutral/45` · default → `text-neutral/45`

## `name`

- Element: `<span data-slot="ai-tool-group-title">` / `<span data-slot="ai-tool-call-title">`
- Base: `min-w-0 flex-1 truncate font-medium text-neutral`

## `status`

- Element: `<span data-slot="ai-tool-status">`
- Base: `shrink-0`

## `content`

- Element: `<div data-slot="ai-tool-call-panel-content">`
- Base: `grid min-w-0 gap-md pb-xs`

## `groupContent`

- Element: `<div data-slot="ai-tool-group-panel-content">`
- Variants: `variant` (card | **ghost** | outline | soft) — bold is the default
- Base: `min-w-0 pl-xs`
- `variant`: card → `pt-md` · ghost → _none_ · outline → `pt-md` · soft → `pt-md`

## `section`

- Element: `<section>`
- Variants: `tone` (default | error) — bold is the default
- Base: `relative grid min-w-0 gap-sm rounded-sm border bg-surface p-md`
- `tone`: default → `border-neutral-muted` · error → `border-danger/20 bg-danger/5`

## `label`

- Element: `<span data-slot="ai-tool-value-label">` / `<div data-slot="ai-tool-value-label">`
- Variants: `kind` (icon | **text**) · `tone` (default | error) — bold is the default
- Base: `flex h-3.5 w-fit items-center`
- `kind`: icon → `absolute top-2 right-2 z-10` · text → `text-xs font-medium tracking-wide uppercase`
- `tone`: default → `text-neutral/70` · error → `text-danger-readable`

## `scrollArea`

- Element: not applied in this component's markup
- Variants: `tone` (default | error) — bold is the default
- Base: `flex max-h-48 min-w-0 flex-col rounded-sm-concentric`
- `tone`: default → `bg-surface-raised/60` · error → `bg-danger/5`

## `scrollViewport`

- Element: not applied in this component's markup
- Base: `max-h-48 w-full`

## `scrollContent`

- Element: not applied in this component's markup
- Base: `min-w-full`

## `scrollScrollbar`

- Element: not applied in this component's markup
- Base: _none_

## `scrollScrollbarX`

- Element: not applied in this component's markup
- Base: _none_

## `scrollThumb`

- Element: not applied in this component's markup
- Variants: `tone` (default | error) — bold is the default
- Base: _none_
- `tone`: default → `bg-neutral-muted` · error → `bg-danger/45`

## `empty`

- Element: `<div data-slot="ai-tool-empty">`
- Base: `py-xs text-xs text-neutral/70`

## `tree`

- Element: `<ul data-slot="ai-tool-value-tree">`
- Base: `grid min-w-full gap-xs p-md text-xs`

## `treeNode`

- Element: `<li data-slot="ai-tool-value-tree-node">`
- Base: `min-w-0`

## `treeBranch`

- Element: `<div data-slot="ai-tool-value-tree-branch">`
- Base: `flex min-w-0 items-baseline gap-md`

## `treeKey`

- Element: `<span data-slot="ai-tool-value-tree-key">`
- Base: `min-w-0 truncate text-neutral/70`

## `treeSummary`

- Element: `<span data-slot="ai-tool-value-tree-summary">`
- Base: `shrink-0 text-xs text-neutral/70`

## `treeChildren`

- Element: `<ul data-slot="ai-tool-value-tree-children">`
- Base: `mt-xs ml-xs grid gap-xs border-l border-neutral-muted pl-lg`

## `treeLeaf`

- Element: `<div data-slot="ai-tool-value-tree-leaf">`
- Base: `grid w-max min-w-full grid-cols-[minmax(4rem,8rem)_max-content] items-baseline gap-lg`

## `treeValue`

- Element: `<span data-slot="ai-tool-value-tree-value">`
- Variants: `kind` (string | number | boolean | null | undefined | empty | unknown) · `tone` (default | error) — bold is the default
- Base: `whitespace-pre font-mono text-xs leading-relaxed`
- `kind`: string → `text-neutral` · number → `text-neutral tabular-nums` · boolean → `text-primary-readable` · null → `text-neutral/70 italic` · undefined → `text-neutral/70 italic` · empty → `text-neutral/70 italic` · unknown → `text-neutral`
- `tone`: default → _none_ · error → `text-danger-readable`
