# field theme parts

Generated by `npm run generate:component-contract` from `Field`'s theme file. Do not edit.

- Registry key: `field`, the key under `<Theme components>`
- Setter: `setFieldTheme` from `entasis/field`
- Parts: 13. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<svelte:element>`
- Variants: `density` (compact | **normal** | comfortable) · `labelPosition` (**top** | left) · `hasError` (true | false) — bold is the default
- Base: `grid min-w-0`
- `density`: compact → `gap-xs [&>legend]:mb-xs` · normal → `gap-md [&>legend]:mb-md` · comfortable → `gap-lg [&>legend]:mb-lg`
- `labelPosition`: top → `grid-cols-1` · left → `@container grid-cols-[minmax(8rem,0.4fr)_minmax(0,1fr)] gap-x-layout-md`
- `hasError`: true → `text-danger-readable` · false → _none_

## `header`

- Element: `<Slot>`
- Variants: `labelPosition` (top | left) · `density` (compact | normal | comfortable) · `required` (false) · `hasError` (true | false) — bold is the default
- Base: `relative flex items-center gap-md`
- `labelPosition`: top → _none_ · left → `col-start-1 col-end-3 @lg:col-end-2 @lg:row-start-1 @lg:self-start @lg:pt-md`
- `density`: compact → `gap-xs` · normal → `gap-md` · comfortable → `gap-lg`
- `required`: false → _none_
- `hasError`: true → `text-danger-readable` · false → _none_

## `label`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) · `hasError` (true | false) · `required` (true | false) — bold is the default
- Base: `text-neutral text-sm`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `hasError`: true → `text-danger-readable` · false → _none_
- `required`: true → `relative before:content-["*"] before:text-danger-readable before:text-sm before:font-bold before:absolute before:-right-2 before:top-0` · false → _none_

## `actions`

- Element: `<Slot>`
- Variants: `density` (compact | normal | comfortable) — bold is the default
- Base: `flex items-start gap-md`
- `density`: compact → `gap-xs` · normal → `gap-md` · comfortable → `gap-lg`

## `errorsContainer`

- Element: `<Slot>`
- Variants: `labelPosition` (top | left) · `size` (small | normal | large) — bold is the default
- Base: `grid gap-xs`
- `labelPosition`: top → _none_ · left → `col-start-1 col-end-3 @lg:col-start-2`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `error`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-danger-readable text-xs leading-3`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `inputContainer`

- Element: `<div>`
- Variants: `labelPosition` (top | left) · `density` (compact | normal | comfortable) · `hasError` (true | false) — bold is the default
- Base: `flex w-full flex-1 items-center justify-between gap-md`
- `labelPosition`: top → _none_ · left → `col-start-1 col-end-3 @lg:col-start-2 @lg:row-start-1 @lg:self-center`
- `density`: compact → `gap-xs` · normal → `gap-md` · comfortable → `gap-lg`
- `hasError`: true → `!ring-danger rounded-md !ring-2 ring-offset-2` · false → _none_

## `prefix`

- Element: `<Slot>`
- Variants: `density` (compact | normal | comfortable) — bold is the default
- Base: `flex items-center gap-md`
- `density`: compact → `gap-xs` · normal → `gap-md` · comfortable → `gap-lg`

## `suffix`

- Element: `<Slot>`
- Variants: `density` (compact | normal | comfortable) — bold is the default
- Base: `flex items-center gap-md`
- `density`: compact → `gap-xs` · normal → `gap-md` · comfortable → `gap-lg`

## `actionButton`

- Element: `<Button>`
- Variants: `size` (small | **normal** | large) · `edge` (start | **end** | none) · `active` (true | **false**) — bold is the default
- Base: `h-auto min-h-0 w-auto self-stretch rounded-none border-0 bg-clip-border !px-0 active:translate-y-0`
- `size`: small → `[&_svg:not([class*=size-])]:size-icon-sm` · normal → `[&_svg:not([class*=size-])]:size-icon-md` · large → `[&_svg:not([class*=size-])]:size-icon-lg`
- `edge`: start → `-ml-lg mr-xs` · end → `ml-xs -mr-lg` · none → `mx-0`
- `active`: true → `text-primary-readable` · false → _none_

## `footer`

- Element: `<Slot>`
- Variants: `labelPosition` (top | left) · `density` (compact | normal | comfortable) — bold is the default
- Base: `flex items-start justify-between gap-md`
- `labelPosition`: top → _none_ · left → `col-start-1 col-end-3 @lg:col-start-2`
- `density`: compact → `gap-xs` · normal → `gap-md` · comfortable → `gap-lg`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 text-xs leading-3 flex-1`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `helper`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 text-xs leading-3`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
