# command theme parts

Generated by `npm run generate:component-contract` from `Command`'s theme file. Do not edit.

- Registry key: `command`, the key under `<Theme components>`
- Setter: `setCommandTheme` from `entasis/command`
- Parts: 14. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `@container bg-transparent text-neutral flex h-full w-full flex-col overflow-hidden rounded-lg p-xs`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `inputWrapper`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `p-xs pb-0`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `inputGroup`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `flex items-center gap-md px-md`
- `size`: small → `h-control-sm` · normal → `h-control-md` · large → `h-control-lg`

## `inputIcon`

- Element: `<magnifyingGlassIcon>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `shrink-0 opacity-50`
- `size`: small → `size-3.5` · normal → `size-4` · large → `size-4.5`

## `input`

- Element: `<input>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `placeholder:text-neutral/70 w-full min-w-0 flex-1 bg-transparent outline-none`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `list`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `scrollbar scrollbar-none max-h-72 flex-1 scroll-py-1 overflow-x-hidden overflow-y-auto outline-none`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `empty`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `py-layout-md text-center`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `group`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral overflow-hidden p-xs`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `groupHeading`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 font-medium`
- `size`: small → `px-md py-xs text-xs` · normal → `px-md py-sm text-xs` · large → `px-md py-md text-sm`

## `separator`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `-mx-xs h-px`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `item`

- Element: `<MenuOption>`
- Variants: `size` (small | **normal** | large) · `highlighted` (true | **false**) — bold is the default
- Base: _none_
- `size`: small → _none_ · normal → _none_ · large → _none_
- `highlighted`: true → _none_ · false → _none_

## `shortcut`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) · `highlighted` (true | **false**) — bold is the default
- Base: `ml-auto hidden tracking-widest @sm:inline`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`
- `highlighted`: true → `text-current/70` · false → `text-neutral/70`

## `footer`

- Element: `<Slot>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `-mx-xs -mb-xs`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `trigger`

- Element: `<Slot>`
- Base: `contents`
