# confirmation theme parts

Generated by `npm run generate:component-contract` from `Confirmation`'s theme file. Do not edit.

- Registry key: `confirmation`, the key under `<Theme components>`
- Setter: `setConfirmationTheme` from `entasis/confirmation`
- Parts: 2. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<Dialog>`
- Base: _none_

## `footer`

- Element: `<div>`
- Variants: `layout` (stacked | **inline**) — bold is the default
- Base: `flex p-md`
- `layout`: stacked → `flex-col gap-xl` · inline → `justify-end gap-md`
