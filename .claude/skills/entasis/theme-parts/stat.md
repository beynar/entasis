# stat theme parts

Generated by `npm run generate:component-contract` from `Stat`'s theme file. Do not edit.

- Registry key: `stat`, the key under `<Theme components>`
- Setter: `setStatTheme` from `entasis/stat`
- Parts: 13. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="stat">`
- Variants: `size` (small | **normal** | large) · `density` (compact | **normal** | comfortable) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `variant` (**solid** | outline | soft | ghost) — bold is the default
- Base: `raised-sm grid grid-cols-[minmax(0,1fr)_auto] border tabular-nums transition-colors`
- `size`: small → `rounded-md` · normal → `rounded-lg` · large → `rounded-lg`
- `density`: compact → `gap-x-lg gap-y-xs p-lg` · normal → `gap-x-xl gap-y-xs p-xl` · comfortable → `gap-x-layout-sm gap-y-sm p-layout-sm`
- `color`: primary → `border-primary` · secondary → `border-secondary` · neutral → `border-neutral` · danger → `border-danger` · success → `border-success` · warning → `border-warning` · info → `border-info`
- `variant`: solid → `border-color bg-color text-color-contrast` · outline → `border-color bg-transparent text-color-readable` · soft → `border-color/20 bg-color-muted text-color-muted-readable` · ghost → `border-transparent bg-transparent text-color-readable shadow-none`
- Compound: color=neutral & variant=solid → `border-neutral-muted bg-surface text-neutral` · color=neutral & variant=outline → `border-neutral-muted text-neutral` · color=neutral & variant=soft → `border-neutral-muted/70 bg-neutral-muted/45 text-neutral-muted-readable` · color=neutral & variant=ghost → `text-neutral`

## `region`

- Element: not applied in this component's markup
- Variants: `span` (narrow | **wide**) — bold is the default
- Base: `col-start-1`
- `span`: narrow → `col-end-2` · wide → `col-end-3`

## `label`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `variant` (solid | soft | outline | ghost) — bold is the default
- Base: `min-w-0 font-medium leading-tight text-current/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `variant`: solid → `text-current` · soft → `text-current` · outline → _none_ · ghost → _none_

## `value`

- Element: `<div data-slot="stat-value">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `gap-xs flex min-w-0 items-baseline font-semibold leading-none tracking-tight text-current`
- `size`: small → `text-xl` · normal → `text-2xl` · large → `text-3xl`

## `unit`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `min-w-0 font-medium leading-none tracking-normal text-current/70`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `aside`

- Element: `<div data-slot="stat-aside">`
- Variants: `rows` (single | **pair**) — bold is the default
- Base: `gap-sm col-start-2 row-start-1 flex flex-col items-end self-start justify-self-end`
- `rows`: single → `row-span-1` · pair → `row-span-2`

## `action`

- Element: `<button data-slot="stat-action">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `state-layer inline-flex shrink-0 items-center justify-center border border-transparent bg-transparent text-current/70 transition-colors focus-visible:ring-2 focus-visible:ring-focus/50 focus-visible:outline-none disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0`
- `size`: small → `size-7 rounded-md [&_svg]:size-icon-sm` · normal → `size-8 rounded-md [&_svg]:size-icon-md` · large → `size-10 rounded-xl [&_svg]:size-icon-lg`

## `indicator`

- Element: `<div data-slot="stat-indicator">`
- Variants: `size` (small | **normal** | large) · `variant` (**default** | icon | badge) · `color` (primary | secondary | **neutral** | danger | success | warning | info) — bold is the default
- Base: `inline-flex shrink-0 items-center justify-center text-current/70 [&_svg]:pointer-events-none [&_svg]:shrink-0`
- `size`: small → `[&_svg]:size-icon-md` · normal → `[&_svg]:size-icon-lg` · large → `[&_svg]:size-icon-xl`
- `variant`: default → _none_ · icon → `border border-current/15 bg-current/5` · badge → `border border-current/15 bg-current/5 font-medium`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · neutral → `text-neutral/70` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable`
- Compound: variant=icon & size=small → `size-7 rounded-md [&_svg]:size-icon-sm` · variant=icon & size=normal → `size-8 rounded-md [&_svg]:size-icon-md` · variant=icon & size=large → `size-10 rounded-xl [&_svg]:size-icon-lg` · variant=badge & size=small → `h-5 min-w-5 rounded-sm px-sm text-xs [&_svg]:size-icon-xs` · variant=badge & size=normal → `h-6 min-w-6 rounded-sm px-md text-xs [&_svg]:size-icon-sm` · variant=badge & size=large → `h-control-sm min-w-7 rounded-md px-md text-sm [&_svg]:size-icon-md`

## `trend`

- Element: `<div data-slot="stat-trend">`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `gap-xs flex min-w-0 items-center font-medium leading-none [&_svg]:pointer-events-none [&_svg]:shrink-0`
- `size`: small → `text-xs [&_svg]:size-icon-xs` · normal → `text-xs [&_svg]:size-icon-xs` · large → `text-sm [&_svg]:size-icon-sm`

## `trendIcon`

- Element: `<Slot>`
- Base: `inline-flex shrink-0 items-center text-current/70`

## `trendText`

- Element: `<span data-slot="stat-trend-text">`
- Variants: `trend` (up | down | **neutral**) — bold is the default
- Base: `gap-xs inline-flex min-w-0 items-center`
- `trend`: up → `text-success-readable` · down → `text-danger-readable` · neutral → `text-current/70`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `variant` (solid | soft | outline | ghost) — bold is the default
- Base: `min-w-0 text-current/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`
- `variant`: solid → `text-current` · soft → `text-current` · outline → _none_ · ghost → _none_

## `separator`

- Element: `<div data-slot="stat-separator">`
- Variants: `density` (compact | **normal** | comfortable) — bold is the default
- Base: _none_
- `density`: compact → `my-xs` · normal → `my-md` · comfortable → `my-lg`
