# tabs theme parts

Generated by `npm run generate:component-contract` from `Tabs`'s theme file. Do not edit.

- Registry key: `tabs`, the key under `<Theme components>`
- Setter: `setTabsTheme` from `entasis/tabs`
- Parts: 3. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: {}, out: {}, duration: 'slow', easing: 'standard' }`

## `root`

- Element: `<div>`
- Variants: `placement` (**top** | bottom | left | right) — bold is the default
- Base: `flex w-full`
- `placement`: top → `flex-col` · bottom → `flex-col-reverse` · left → `flex-row` · right → `flex-row-reverse`

## `content`

- Element: `<Stepper>`
- Variants: `placement` (**top** | bottom | left | right) — bold is the default
- Base: `w-full  flex-full`
- `placement`: top → `w-full` · bottom → `w-full` · left → `h-full` · right → `h-full`
