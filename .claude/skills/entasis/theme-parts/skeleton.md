# skeleton theme parts

Generated by `npm run generate:component-contract` from `Skeleton`'s theme file. Do not edit.

- Registry key: `skeleton`, the key under `<Theme components>`
- Setter: `setSkeletonTheme` from `entasis/skeleton`
- Parts: 1. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="skeleton">`
- Variants: `color` (primary | secondary | danger | success | warning | info | **neutral**) — bold is the default
- Base: `animate-pulse rounded-sm`
- `color`: primary → `bg-primary-light/10` · secondary → `bg-secondary-light/10` · danger → `bg-danger-light/10` · success → `bg-success-light/10` · warning → `bg-warning-light/10` · info → `bg-info-light/10` · neutral → `bg-neutral/10`
