# sortable-list theme parts

Generated by `npm run generate:component-contract` from `SortableList`'s theme file. Do not edit.

- Registry key: `sortable-list`, the key under `<Theme components>`
- Setter: `setSortableListTheme` from `entasis/sortable-list`
- Parts: 5. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<ul>`
- Variants: `size` (small | **normal** | large) · `orientation` (**vertical** | horizontal | grid) — bold is the default
- Base: `m-0 flex list-none p-0`
- `size`: small → `gap-xs` · normal → `gap-md` · large → `gap-lg`
- `orientation`: vertical → `flex-col` · horizontal → `flex-row` · grid → `flex-row flex-wrap`

## `item`

- Element: `<li>`
- Variants: `size` (small | **normal** | large) · `handle` (true | **false**) · `dragging` (true | **false**) · `disabled` (true | **false**) — bold is the default
- Base: `border-neutral-muted bg-surface text-neutral relative flex items-center rounded-lg border outline-none transition-[box-shadow,opacity] focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `gap-md p-md text-xs` · normal → `gap-lg p-lg text-sm` · large → `gap-xl p-xl text-base`
- `handle`: true → _none_ · false → `cursor-grab touch-none select-none active:cursor-grabbing`
- `dragging`: true → `opacity-40` · false → _none_
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
- Compound: disabled=true & handle=false → `cursor-not-allowed active:cursor-not-allowed`

## `content`

- Element: `<Slot>`
- Base: `min-w-0 flex-1`

## `handle`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 hover:text-neutral inline-flex shrink-0 cursor-grab touch-none items-center justify-center rounded-sm outline-none transition-colors focus-visible:ring-2 focus-visible:ring-focus/50 active:cursor-grabbing disabled:pointer-events-none disabled:cursor-not-allowed`
- `size`: small → `size-6` · normal → `size-7` · large → `size-8`

## `empty`

- Element: `<li>`
- Base: `text-neutral/70 border-neutral-muted flex min-h-row-lg w-full items-center justify-center rounded-lg border border-dashed px-lg py-md text-sm`
