# toast theme parts

Generated by `npm run generate:component-contract` from `Toast`'s theme file. Do not edit.

- Registry key: `toast`, the key under `<Theme components>`
- Setter: `setToastTheme` from `entasis/toast`
- Parts: 11. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: { x: 0, y: 0, opacity: 0 }, out: { x: 0, y: 0, opacity: 0 } }`
- `position`: 'top-left' → `{ in: { y: -100, x: -100 }, out: { y: -100, x: -100 } }` · 'top-right' → `{ in: { y: -100, x: 100 }, out: { y: -100, x: 100 } }` · 'top-center' → `{ in: { y: -100 }, out: { y: -100 } }` · 'bottom-left' → `{ in: { y: 100, x: -100 }, out: { y: 100, x: -100 } }` · 'bottom-right' → `{ in: { y: 100, x: 100 }, out: { y: 100, x: 100 } }` · 'bottom-center' → `{ in: { y: 100 }, out: { y: 100 } }` · 'banner-top' → `{ in: { y: '-100%' }, out: { y: '-100%' } }` · 'banner-bottom' → `{ in: { y: '100%' }, out: { y: '100%' } }`

## `toaster`

- Element: `<section>`
- Base: `pointer-events-none fixed inset-0 z-[9999] m-0 h-full w-full max-h-none max-w-none overflow-hidden border-0 bg-transparent p-0`

## `root`

- Element: `<li>`
- Variants: `richColors` (true | false) · `color` (primary | secondary | neutral | danger | success | warning | info) · `size` (small | **normal** | large) · `banner` (true | **false**) — bold is the default
- Base: `cursor-default absolute pointer-events-auto bg-surface-floating border border-neutral-muted text-neutral lift-1 transition-[translate,scale,opacity] duration-slower flex items-start justify-between`
- `richColors`: true → `bg-color-muted text-color-muted-readable border-color/20` · false → _none_
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `size`: small → `gap-md rounded-md px-md py-md min-w-[220px] max-w-[300px]` · normal → `gap-md rounded-lg px-lg py-md min-w-[260px] max-w-[340px]` · large → `gap-lg rounded-lg px-xl py-lg min-w-[300px] max-w-[400px]`
- `banner`: true → `w-full min-w-0 max-w-none rounded-none border-x-0 items-center lift-5` · false → _none_

## `prefix`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | neutral | danger | success | warning | info) · `richColors` (true | false) — bold is the default
- Base: `aspect-square flex items-center justify-center text-color-readable`
- `size`: small → `size-4 [&_svg]:size-icon-md` · normal → `size-5 [&_svg]:size-icon-lg` · large → `size-6 [&_svg]:size-icon-xl`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `richColors`: true → _none_ · false → _none_

## `actions`

- Element: `<div>`
- Base: `flex shrink-0 items-center gap-xs`

## `progress`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `banner` (true | **false**) — bold is the default
- Base: `pointer-events-none absolute inset-x-0 bottom-0 h-1 overflow-hidden bg-neutral/15`
- `size`: small → `rounded-b-md` · normal → `rounded-b-lg` · large → `rounded-b-lg`
- `banner`: true → `rounded-none` · false → _none_

## `suffix`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | neutral | danger | success | warning | info) · `richColors` (true | false) — bold is the default
- Base: `max-h-10`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `richColors`: true → _none_ · false → _none_

## `content`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | neutral | danger | success | warning | info) · `richColors` (true | false) — bold is the default
- Base: `grid flex-1`
- `size`: small → `gap-0` · normal → `gap-micro` · large → `gap-xs`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `richColors`: true → _none_ · false → _none_

## `closeIcon`

- Element: `<button>`
- Variants: `size` (small | **normal** | large) · `richColors` (true | false) · `color` (primary | secondary | neutral | danger | success | warning | info) — bold is the default
- Base: `state-layer shrink-0 -mr-xs -mt-micro flex items-center justify-center rounded-sm leading-none transition-colors outline-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `size-4 [&_svg]:size-icon-xs` · normal → `size-5 [&_svg]:size-icon-sm` · large → `size-6 [&_svg]:size-icon-md`
- `richColors`: true → `text-color-muted-readable/70 hover:text-color-muted-readable` · false → `text-neutral/70 hover:text-neutral`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_

## `title`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | neutral | danger | success | warning | info) · `richColors` (true | false) — bold is the default
- Base: `font-semibold leading-tight`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `richColors`: true → `text-color-muted-readable` · false → `text-neutral`

## `description`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | neutral | danger | success | warning | info) · `richColors` (true | false) — bold is the default
- Base: `leading-snug text-neutral/70`
- `size`: small → `text-xs` · normal → `text-xs` · large → `text-sm`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `richColors`: true → `text-current/70` · false → _none_
