# date-selector theme parts

Generated by `npm run generate:component-contract` from `DateSelector`'s theme file. Do not edit.

- Registry key: `date-selector`, the key under `<Theme components>`
- Setter: `setDateSelectorTheme` from `entasis/date-selector`
- Parts: 5. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `popover`

- Element: `<Popover>`
- Base: `w-auto max-w-[calc(100vw-2rem)] overflow-hidden p-0`

## `root`

- Element: `<div>`
- Variants: `withPresets` (true | **false**) · `view` (**single** | double) — bold is the default
- Base: `grid max-w-full items-stretch`
- `withPresets`: true → `sm:grid-cols-[9rem_minmax(0,1fr)]` · false → `grid-cols-1`
- `view`: single → _none_ · double → _none_
- Compound: withPresets=false & view=single → `w-[18rem]` · withPresets=true & view=single → `w-[28rem]` · withPresets=false & view=double → `w-[36rem]` · withPresets=true & view=double → `w-[46rem]`

## `presets`

- Element: `<div>`
- Base: `border-neutral-muted flex flex-wrap content-start gap-xs border-b p-md sm:flex-col sm:border-r sm:border-b-0`

## `preset`

- Element: `<Button>`
- Base: `justify-start text-left`

## `calendar`

- Element: not applied in this component's markup
- Base: `min-w-0 rounded-none border-0 bg-transparent`
