# image-gallery theme parts

Generated by `npm run generate:component-contract` from `ImageGallery`'s theme file. Do not edit.

- Registry key: `image-gallery`, the key under `<Theme components>`
- Setter: `setImageGalleryTheme` from `entasis/image-gallery`
- Parts: 3. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: {}, out: {}, duration: 'normal', easing: 'enter' }`

## `root`

- Element: `<div>`
- Base: `contents [&_[data-image-gallery-image]]:cursor-zoom-in`

## `caption`

- Element: `<Slot>`
- Base: `text-neutral bg-surface-floating/90 fixed bottom-20 left-1/2 z-[1100] max-w-[min(44rem,calc(100vw-2rem))] -translate-x-1/2 rounded-full px-xl py-md text-center text-sm lift-1 backdrop-blur-md`
