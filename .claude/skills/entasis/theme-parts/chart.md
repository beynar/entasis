# chart theme parts

Generated by `npm run generate:component-contract` from `Chart`'s theme file. Do not edit.

- Registry key: `chart`, the key under `<Theme components>`
- Setter: `setChartTheme` from `entasis/chart`
- Parts: 7. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `motion`

- Reserved `motion` slot, a transition spec rather than classes. Base: `{ in: {}, out: {}, duration: 'slow', easing: 'enter' }`

## `root`

- Element: not applied in this component's markup
- Base: `relative flex h-80 w-full min-w-0 flex-col text-neutral`

## `plot`

- Element: not applied in this component's markup
- Base: `relative min-h-0 w-full min-w-0 flex-1 [&_.ts-chart]:overflow-visible`

## `legend`

- Element: not applied in this component's markup
- Variants: `placement` (top | **bottom**) · `align` (**left** | center | right) · `orientation` (**horizontal** | vertical) — bold is the default
- Base: `flex shrink-0 flex-wrap gap-xs`
- `placement`: top → `order-first pb-sm` · bottom → `order-last pt-sm`
- `align`: left → _none_ · center → _none_ · right → _none_
- `orientation`: horizontal → `flex-row items-center` · vertical → `flex-col`
- Compound: orientation=horizontal & align=left → `justify-start` · orientation=horizontal & align=center → `justify-center` · orientation=horizontal & align=right → `justify-end` · orientation=vertical & align=left → `items-start` · orientation=vertical & align=center → `items-center` · orientation=vertical & align=right → `items-end`

## `legendItem`

- Element: not applied in this component's markup
- Base: `inline-flex shrink-0 items-center gap-sm px-md text-xs font-medium whitespace-nowrap text-neutral/70`

## `legendSwatch`

- Element: not applied in this component's markup
- Base: `inline-block size-2.5 shrink-0 rounded-xs`

## `tooltip`

- Element: not applied in this component's markup
- Base: `z-50 !max-w-80 !rounded-lg !border-neutral-muted !bg-surface-floating !px-lg !py-md !font-sans !text-xs !leading-tight !font-medium !text-neutral !lift-5`
