# pagination theme parts

Generated by `npm run generate:component-contract` from `Pagination`'s theme file. Do not edit.

- Registry key: `pagination`, the key under `<Theme components>`
- Setter: `setPaginationTheme` from `entasis/pagination`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<nav data-slot="pagination">`
- Base: `flex w-fit max-w-full flex-wrap items-center gap-lg`

## `list`

- Element: `<ul>`
- Variants: `size` (small | **normal** | large) · `variant` (**pages** | count | compact | dots | none) — bold is the default
- Base: `flex flex-wrap items-center`
- `size`: small → `gap-xs` · normal → `gap-sm` · large → `gap-md`
- `variant`: pages → _none_ · count → `gap-md` · compact → `gap-md` · dots → `gap-0` · none → `gap-md`

## `item`

- Element: `<li>`
- Variants: `size` (small | **normal** | large) · `display` (**page** | dot) — bold is the default
- Base: `flex shrink-0 items-center`
- `size`: small → _none_ · normal → _none_ · large → _none_
- `display`: page → _none_ · dot → `justify-center`
- Compound: size=small & display=dot → `size-5` · size=normal & display=dot → `size-6` · size=large & display=dot → `size-7`

## `control`

- Element: `<svelte:element>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `controlVariant` (solid | outline | soft | **ghost**) · `active` (true | **false**) · `disabled` (true | **false**) · `control` (**page** | icon) — bold is the default
- Base: `inline-flex shrink-0 select-none items-center justify-center rounded-md border font-medium outline-none transition-[color,background-color,border-color,box-shadow,translate] duration-fast ease-standard focus-visible:ring-2 focus-visible:ring-focus/50 [&:active]:translate-y-px [&_svg:not([class*=size-])]:size-icon-md`
- `size`: small → `h-control-sm min-size-hit-sm px-md text-xs` · normal → `h-control-md min-size-hit-md px-lg text-sm` · large → `h-control-lg min-size-hit-lg px-xl text-sm`
- `color`: primary → `text-primary-readable` · secondary → `text-secondary-readable` · neutral → `text-neutral-readable` · danger → `text-danger-readable` · success → `text-success-readable` · warning → `text-warning-readable` · info → `text-info-readable`
- `controlVariant`: solid → `state-layer border-transparent bg-color text-color-contrast` · outline → `state-layer bg-color/0 border-color text-color-readable` · soft → `state-layer border-transparent bg-color-muted text-color-muted-readable` · ghost → `state-layer border-transparent bg-transparent text-color-readable`
- `active`: true → _none_ · false → _none_
- `disabled`: true → `pointer-events-none cursor-not-allowed opacity-50` · false → `cursor-pointer`
- `control`: page → _none_ · icon → `aspect-square px-0`
- Compound: color=neutral & controlVariant=outline → `border-neutral-muted`

## `dot`

- Element: `<svelte:element>`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `active` (true | **false**) · `disabled` (true | **false**) — bold is the default
- Base: `relative inline-flex shrink-0 items-center justify-center rounded-full outline-none transition-colors before:block before:rounded-full before:content-[''] before:transition-colors focus-visible:ring-2 focus-visible:ring-focus/50`
- `size`: small → `size-3 before:size-1.5` · normal → `size-4 before:size-2` · large → `size-5 before:size-2.5`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `active`: true → `before:bg-selected` · false → `before:bg-neutral/30 hover:before:bg-color/50`
- `disabled`: true → `pointer-events-none cursor-not-allowed opacity-50` · false → `cursor-pointer`

## `icon`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex shrink-0 items-center justify-center`
- `size`: small → `size-icon-sm` · normal → `size-icon-md` · large → `size-icon-lg`

## `ellipsis`

- Element: `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 inline-flex shrink-0 items-center justify-center`
- `size`: small → `h-control-sm min-w-[var(--control-height-sm)]` · normal → `h-control-md min-w-[var(--control-height-md)]` · large → `h-control-lg min-w-[var(--control-height-lg)]`

## `summary`

- Element: `<Slot>` / `<span>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `text-neutral/70 shrink-0 whitespace-nowrap font-medium`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
