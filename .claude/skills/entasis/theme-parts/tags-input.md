# tags-input theme parts

Generated by `npm run generate:component-contract` from `TagsInput`'s theme file. Do not edit.

- Registry key: `tags-input`, the key under `<Theme components>`
- Setter: `setTagsInputTheme` from `entasis/tags-input`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `outline-none flex-1 min-w-24 min-h-0 bg-transparent resize-none autofill:text-neutral appearance-none text-sm leading-normal`
- `size`: small → `text-xs placeholder:text-xs` · normal → `text-sm placeholder:text-sm` · large → `text-sm placeholder:text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | **false**) — bold is the default
- Base: `flex w-full flex-wrap items-center justify-start border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `min-h-[var(--control-height-sm)] py-xs text-xs gap-xs` · normal → `min-h-[var(--control-height-md)] py-xs text-sm gap-sm` · large → `min-h-[var(--control-height-lg)] py-sm text-sm gap-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `tag`

- Element: `<li>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `inline-flex`
- `size`: small → _none_ · normal → _none_ · large → _none_

## `loading`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 p-md text-sm`
- `size`: small → `text-xs p-sm` · normal → `text-sm p-sm` · large → `text-sm p-md`

## `error`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-danger-readable p-md text-sm`
- `size`: small → `text-xs p-sm` · normal → `text-sm p-sm` · large → `text-sm p-md`

## `noOptions`

- Element: `<div>`
- Variants: `size` (small | normal | large) — bold is the default
- Base: `text-neutral/70 p-md text-sm`
- `size`: small → `text-xs p-sm` · normal → `text-sm p-sm` · large → `text-sm p-md`
