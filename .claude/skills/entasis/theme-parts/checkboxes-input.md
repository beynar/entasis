# checkboxes-input theme parts

Generated by `npm run generate:component-contract` from `CheckboxesInput`'s theme file. Do not edit.

- Registry key: `checkboxes-input`, the key under `<Theme components>`
- Setter: `setCheckboxesInputTheme` from `entasis/checkboxes-input`
- Parts: 8. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<Field>`
- Variants: `mode` (card | normal | control) — bold is the default
- Base: _none_
- `mode`: card → _none_ · normal → _none_ · control → `inline-flex`

## `checkboxesInputItem`

- Element: `<button>`
- Variants: `mode` (card | normal | control) · `size` (small | **normal** | large) · `checked` (true | false) · `disabled` (true | false) — bold is the default
- Base: `relative grid w-full cursor-pointer items-start gap-xs rounded-md pl-layout-xl text-left transition-[color,background-color,box-shadow,opacity] outline-none focus-visible:ring-2 focus-visible:ring-focus/50`
- `mode`: card → `raised bg-surface-raised py-md` · normal → `py-xs` · control → `!inline-grid !min-h-0 !grid-cols-1 !place-items-center !gap-0 !p-0 !pl-0`
- `size`: small → `min-h-row-sm` · normal → `min-h-row-md` · large → `min-h-row-lg`
- `checked`: true → _none_ · false → _none_
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_
- Compound: mode=card & checked=true → `ring-2 ring-selected bg-selected-muted text-selected-muted-readable` · mode=control & size=small → `!size-4 !w-4` · mode=control & size=normal → `!size-5 !w-5` · mode=control & size=large → `!size-6 !w-6`

## `checkboxesInputItemLabel`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex items-center gap-xl flex-wrap`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-base`

## `checkboxesInputItemTrack`

- Element: `<div>`
- Variants: `checked` (true | false) · `size` (small | normal | large) · `mode` (card | normal | control) · `disabled` (true | false) — bold is the default
- Base: `origin-center content-[""] bg-surface-raised border border-neutral-muted rounded-sm absolute left-2`
- `checked`: true → _none_ · false → _none_
- `size`: small → `size-4` · normal → `size-5` · large → `size-6`
- `mode`: card → `top-2` · normal → `top-1` · control → `!static col-start-1 row-start-1`
- `disabled`: true → _none_ · false → _none_

## `checkboxesInputItemThumb`

- Element: `<div>`
- Variants: `checked` (true | false) · `size` (small | normal | large) · `mode` (card | normal | control) · `disabled` (true | false) — bold is the default
- Base: `origin-center radio rounded-sm flex items-center justify-center transition-[background-color,opacity,scale] content-[""] absolute left-2 scale-[85%] opacity-0 p-xs text-selected-contrast`
- `checked`: true → `bg-selected scale-[100%] opacity-100` · false → _none_
- `size`: small → `size-4` · normal → `size-5` · large → `size-6`
- `mode`: card → `top-2` · normal → `top-1` · control → `!static col-start-1 row-start-1`
- `disabled`: true → _none_ · false → _none_

## `checkboxesInputItemIcon`

- Element: not applied in this component's markup
- Base: _none_

## `checkboxesInputItemDescription`

- Element: `<Slot>`
- Variants: `mode` (card | normal | control) · `checked` (true | false) — bold is the default
- Base: `text-xs text-neutral/70`
- `mode`: card → _none_ · normal → _none_ · control → _none_
- `checked`: true → _none_ · false → _none_
- Compound: mode=card & checked=true → `text-color-muted-readable`

## `checkboxesInputContainer`

- Element: not applied in this component's markup
- Variants: `mode` (card | normal | control) · `disabled` (true | false) — bold is the default
- Base: `grid gap-lg grid-cols-1 items-start`
- `mode`: card → _none_ · normal → _none_ · control → `!inline-flex !w-auto !flex-none`
- `disabled`: true → _none_ · false → _none_
