# file-input theme parts

Generated by `npm run generate:component-contract` from `FileInput`'s theme file. Do not edit.

- Registry key: `file-input`, the key under `<Theme components>`
- Setter: `setFileInputTheme` from `entasis/file-input`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `state` (idle | potential | valid | invalid) · `disabled` (true | false) — bold is the default
- Base: `flex min-h-32 w-full cursor-pointer flex-col items-center justify-center border border-neutral-muted bg-surface-raised px-lg py-md text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `min-h-24` · normal → `min-h-32` · large → `min-h-40`
- `state`: idle → _none_ · potential → `border-neutral/50` · valid → `border-success bg-success/5` · invalid → `border-danger bg-danger/5`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `placeholder`

- Element: `<div>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex flex-col items-center justify-center gap-md text-neutral/70`
- `size`: small → `text-xs gap-xs` · normal → `text-sm gap-md` · large → `text-base gap-lg`

## `fileList`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `w-full flex flex-col gap-md mt-md`
- `size`: small → `gap-xs` · normal → `gap-md` · large → `gap-lg`

## `file`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: `flex items-center gap-lg p-md bg-surface-canvas`
- `size`: small → `p-xs text-xs gap-md` · normal → `p-md text-sm gap-lg` · large → `p-lg text-base gap-xl`
