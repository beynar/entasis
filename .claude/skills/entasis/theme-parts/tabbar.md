# tabbar theme parts

Generated by `npm run generate:component-contract` from `Tabbar`'s theme file. Do not edit.

- Registry key: `tabbar`, the key under `<Theme components>`
- Setter: `setTabbarTheme` from `entasis/tabbar`
- Parts: 6. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div data-slot="tabbar">`
- Variants: `orientation` (**horizontal** | vertical) · `scrollFade` (**none** | x | y) · `alignment` (**start** | center | end) · `size` (small | **normal** | large) · `variant` (**underline** | pill) · `fullWidth` (true | false) — bold is the default
- Base: `relative flex w-fit max-w-full`
- `orientation`: horizontal → `flex-row overflow-x-auto scrollbar-none` · vertical → `flex-col overflow-y-auto scrollbar-none`
- `scrollFade`: none → _none_ · x → `scroll-fade-x` · y → `scroll-fade-y`
- `alignment`: start → `justify-start` · center → `justify-center` · end → `justify-end`
- `size`: small → `gap-micro` · normal → `gap-xs` · large → `gap-sm`
- `variant`: underline → _none_ · pill → `rounded-full bg-neutral-muted/60 p-xs`
- `fullWidth`: true → `w-full` · false → _none_
- Compound: variant=underline & orientation=horizontal → `shadow-[inset_0_-1px_0_0_var(--color-neutral-muted)]` · variant=underline & orientation=vertical → `shadow-[inset_-1px_0_0_0_var(--color-neutral-muted)]` · variant=pill & orientation=vertical → `rounded-lg`

## `tab`

- Element: `<button data-slot="tabbar-tab">` / `<svelte:element data-slot="tabbar-tab">`
- Variants: `size` (small | **normal** | large) · `color` (primary | secondary | **neutral** | danger | success | warning | info) · `active` (true | **false**) · `focused` (true | **false**) · `disabled` (true | **false**) · `orientation` (**horizontal** | vertical) · `position` (**top** | bottom | left | right) · `variant` (**underline** | pill) · `fullWidth` (true | false) — bold is the default
- Base: `rounded-sm relative cursor-pointer inline-flex items-center justify-center outline-none whitespace-nowrap text-neutral/70 transition-colors duration-normal focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-focus/50`
- `size`: small → `px-md py-xs text-xs gap-xs` · normal → `px-lg py-xs text-sm gap-md` · large → `px-xl py-xs text-sm gap-md`
- `color`: primary → _none_ · secondary → _none_ · neutral → _none_ · danger → _none_ · success → _none_ · warning → _none_ · info → _none_
- `active`: true → _none_ · false → _none_
- `focused`: true → `text-neutral` · false → _none_
- `disabled`: true → `opacity-50 cursor-not-allowed pointer-events-none` · false → _none_
- `orientation`: horizontal → _none_ · vertical → `w-full`
- `position`: top → _none_ · bottom → _none_ · left → _none_ · right → _none_
- `variant`: underline → _none_ · pill → `rounded-full`
- `fullWidth`: true → `w-full` · false → _none_
- Compound: active=true & variant=underline → `text-neutral` · active=true & variant=pill → `text-selected-contrast`

## `indicator`

- Element: `<div data-slot="tabbar-indicator">`
- Variants: `variant` (**underline** | pill) — bold is the default
- Base: `pointer-events-none absolute left-0 top-0 will-change-transform data-[ready=true]:transition-[transform,width,height] data-[ready=true]:duration-slow data-[ready=true]:ease-standard`
- `variant`: underline → `rounded-full bg-selected` · pill → `rounded-full bg-selected lift-1`

## `staticIndicator`

- Element: `<span data-slot="tabbar-indicator">`
- Variants: `variant` (**underline** | pill) · `position` (**top** | bottom | left | right) — bold is the default
- Base: `pointer-events-none absolute bg-selected`
- `variant`: underline → `rounded-full` · pill → `inset-0 -z-10 rounded-full lift-1`
- `position`: top → _none_ · bottom → _none_ · left → _none_ · right → _none_
- Compound: variant=underline & position=top → `inset-x-0 bottom-0 h-0.5` · variant=underline & position=bottom → `inset-x-0 top-0 h-0.5` · variant=underline & position=left → `inset-y-0 right-0 w-0.5` · variant=underline & position=right → `inset-y-0 left-0 w-0.5`

## `prefix`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: _none_
- `size`: small → `[&>svg]:size-icon-sm` · normal → `[&>svg]:size-icon-md` · large → `[&>svg]:size-icon-lg`

## `suffix`

- Element: `<Slot>`
- Variants: `size` (small | **normal** | large) — bold is the default
- Base: _none_
- `size`: small → `[&>svg]:size-icon-sm` · normal → `[&>svg]:size-icon-md` · large → `[&>svg]:size-icon-lg`
