# toggle-menu theme parts

Generated by `npm run generate:component-contract` from `ToggleMenu`'s theme file. Do not edit.

- Registry key: `toggle-menu`, the key under `<Theme components>`
- Setter: `setToggleMenuTheme` from `entasis/toggle-menu`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `root`

- Element: `<div>` / `<button>`
- Base: `border-neutral-muted bg-neutral-muted/40 relative inline-flex max-w-full items-center gap-xs rounded-md border p-xs`

## `rail`

- Element: `<div>`
- Base: `flex min-w-0 items-center gap-md overflow-hidden whitespace-nowrap`

## `unit`

- Element: `<ToggleMenuGroup>` / `<ToggleMenuRadioGroup>` / `<ToggleMenuMenu>` / `<span>`
- Base: `shrink-0 data-[overflowed=true]:pointer-events-none data-[overflowed=true]:invisible`

## `more`

- Element: `<span>`
- Base: `shrink-0 data-[overflow=false]:pointer-events-none data-[overflow=false]:absolute data-[overflow=false]:right-1 data-[overflow=false]:invisible`
