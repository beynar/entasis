# color-input theme parts

Generated by `npm run generate:component-contract` from `ColorInput`'s theme file. Do not edit.

- Registry key: `color-input`, the key under `<Theme components>`
- Setter: `setColorInputTheme` from `entasis/color-input`
- Parts: 4. Each part takes `base` plus one map per variant in the `theme` prop, the setter and the registry alike.
- Override order: your classes land after the part's base, variant and compound classes, so a plain Tailwind class in an override's `base` beats any of them; the instance `class` prop lands last.

## `input`

- Element: `<input>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `outline-none flex-1 w-full min-w-0 h-full bg-transparent resize-none placeholder:text-neutral/70 autofill:text-neutral appearance-none font-mono text-sm leading-normal [word-spacing:-0.5ch]`
- `size`: small → `text-xs` · normal → `text-sm` · large → `text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `inputContainer`

- Element: `<Field>`
- Variants: `size` (small | **normal** | large) · `disabled` (true | false) — bold is the default
- Base: `flex w-full items-center border border-neutral-muted bg-surface-raised px-lg text-neutral ring-0 transition-[color,background-color,border-color,box-shadow] rounded-md outline-none focus-within:ring-2 focus-within:ring-focus/50`
- `size`: small → `h-control-sm text-xs` · normal → `h-control-md text-sm` · large → `h-control-lg text-sm`
- `disabled`: true → `cursor-not-allowed opacity-50` · false → _none_

## `popover`

- Element: `<Popover>`
- Base: `w-auto max-w-[calc(100vw-2rem)] p-0`

## `swatch`

- Element: `<span>`
- Variants: `empty` (**true** | false) — bold is the default
- Base: `relative size-full overflow-hidden rounded-sm border border-neutral-muted`
- `empty`: true → `bg-neutral-muted` · false → `bg-[repeating-conic-gradient(#c7c7c7_0%_25%,#fff_0%_50%)] bg-[length:8px_8px]`
