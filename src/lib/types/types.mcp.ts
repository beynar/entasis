export const typesDescription = `
# Shared theme types

Import from \`svelai/types\`.

Colors names semantic palette roles. Sizes selects small, normal, or large component geometry. Density uses the same scale for internal whitespace. The module also exports theme color paths, typography paths, transition easing, style types, and deepMerge for composing nested theme values.
`;
