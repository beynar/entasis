import type { ScrollAreaThemeProps } from '../ScrollArea/scrollArea.theme.js';

export const aiThreadTocScrollAreaTheme = {
	content: { base: 'h-full' },
	scrollbar: { base: '!hidden' },
	scrollbarX: { base: '!hidden' }
} satisfies ScrollAreaThemeProps;
