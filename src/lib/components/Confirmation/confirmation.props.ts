import type { ConfirmationThemeProps } from './confirmation.theme.js';

export type ConfirmationProps = {
	/**
	 * Theme overrides for the confirmation dialog surface and its button row.
	 */
	theme?: ConfirmationThemeProps;
};
