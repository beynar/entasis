should accept
attachements props
theme props with key for button option and separator
array of items that can then render
{type "button" } & ButtonProps
{type "option"} & MenuOptionProps
{type "separator" }& SeparatorProps

in term of styling this should be pretty minimal as it is just a div iterating on an aray of items (mainly mananaging spacing between items)
